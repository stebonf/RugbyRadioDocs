# Backend Data Map RAW

## Sintesi

- Data analisi: 2026-05-19
- Root backend analizzata: `src/RugbyRadio`
- Filtro applicato: soli file modificati dopo il 2026-05-14
- File output: `llm-wiki/raw/backend/api/data-map-20260519.md`
- DbContext trovati: 0
- Entity trovate: 3
- Repository trovati: 3
- Enum dati trovati: 1

## File analizzati

- `src/RugbyRadio/HF/Dto/SeoPublicUrl.cs`
- `src/RugbyRadio/HF/Dto/SeoPublicUrlRules.cs`
- `src/RugbyRadio/HF/Services/SeoUrlInventoryService.cs`
- `src/RugbyRadio/Lib/Repositories/ChannelBox/ChannelRepository.cs`
- `src/RugbyRadio/Lib/Repositories/ChannelBox/IChannelRepository.cs`
- `src/RugbyRadio/Lib/Repositories/MatchBox/IMatchRepository.cs`
- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchRepository.cs`
- `src/RugbyRadio/Lib/Repositories/TeamBox/ITeamRepository.cs`
- `src/RugbyRadio/Lib/Repositories/TeamBox/TeamRepository.cs`
- `src/RugbyRadio/Lib/Settings/SeoSitemapSettings.cs`

## File modificati esclusi perché non rilevanti per modello dati persistente

- `src/RugbyRadio/Api/Api.xml` - documentazione XML generata, non sorgente dati.
- `src/RugbyRadio/HF/appsettings.json` - configurazione applicativa, non modello persistente.
- `src/RugbyRadio/HF/Helpers/SitemapXmlWriter.cs` - writer XML sitemap, non persistenza applicativa.
- `src/RugbyRadio/HF/Jobs/Core/BaseCoreJob.cs` - infrastruttura job, non modello dati.
- `src/RugbyRadio/HF/Jobs/CreateMatchBlogHtmlJob.cs` - job applicativo, non analizzato in dettaglio.
- `src/RugbyRadio/HF/Jobs/GenerateSeoSitemapJob.cs` - job applicativo, non analizzato in dettaglio.
- `src/RugbyRadio/HF/Program.cs` - bootstrap applicativo, non modello dati.
- `src/RugbyRadio/HF/Templates/post-template.html` - template HTML.
- `src/RugbyRadio/HF/Services/ISeoUrlInventoryService.cs` - interfaccia service, non repository.

## DbContext

Nessun DbContext modificato dopo il 2026-05-14 nel perimetro analizzato.

## Entity

## Channel

### Nome wiki suggerito

`ChannelEntity`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/ChannelBox/Channel.cs`

### Namespace

Non deducibile dai soli file modificati analizzati.

### Tipo

Entity persistente dedotta da `IRepository<Channel>` e query EF in `ChannelRepository`.

### Identificativo

- Proprietà chiave: `Id` (`string`) - dedotta da `GetByIdAsync(string id)`.
- Altri identificativi:
  - `PublicId` (`string?`) - usato da `GetByPublicIdIncludeAsync`, `GetByPublicIdAsync`, `FindSeoCandidatesAsync`.

### Proprietà

- `Id` (`string`) - obbligatorietà non deducibile. Usata come identificativo repository.
- `PublicId` (`string?`) - opzionale deducibile da controllo `PublicId != null` e `!string.IsNullOrEmpty(PublicId)`.
- `UserId` (`string?`) - opzionale deducibile da controllo `UserId != null`.
- `Name` (`string?`) - opzionale/non deducibile. Usata in ricerca testuale.
- `HeaderTitle` (`string?`) - opzionale/non deducibile. Usata in ricerca testuale.
- `HeaderSubtitle` (`string?`) - opzionale/non deducibile. Usata in ricerca testuale.
- `IsDeleted` (`bool`) - obbligatorietà non deducibile. Usata come filtro applicativo.
- `IsTestChannel` (`bool?`) - opzionale deducibile da `HasValue` e confronto con `true`.
- `Ts` (`DateTime`) - obbligatorietà non deducibile. Usata da `SeoUrlInventoryService` per calcolo `lastmod`.

### Relazioni

- `User` - Tipo relazione: navigation. Evidenza: `Include(x => x.User)` e `x.User.Nickname`.
- `ChannelUser` / owners - Tipo relazione: collection navigation, cardinalità non deducibile. Evidenza: `Include(x => x.Owners)` e `x.Owners.Any(y => y.UserId == userId.ToUpper())`.
- `Match` - Tipo relazione: collection navigation, cardinalità non deducibile. Evidenza: `Include(x => x.Matches)` e `channel.Matches`.

### Enum usati

- Nessun enum deducibile dai file modificati analizzati.

### Validator associati

- `IValidator<Channel>` iniettato nel costruttore di `ChannelRepository`.
- Regole validator: non deducibili dai soli file modificati analizzati.

### Note di confidenza

- Parzialmente dedotto dai repository modificati.

## Match

### Nome wiki suggerito

`MatchEntity`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/Match.cs`

### Namespace

Non deducibile dai soli file modificati analizzati.

### Tipo

Entity persistente dedotta da `IRepository<Match>` e query EF in `MatchRepository`.

### Identificativo

- Proprietà chiave: `Id` (`string`) - dedotta da `GetByIdIncludeAsync(string matchId)` e URL SEO `/g-match/{match.Id}`.
- Altri identificativi:
  - `ChannelId` (`string?`) - foreign key dedotta da filtri repository.
  - `HomeTeamId` (`string?`) - foreign key dedotta da policy SEO.
  - `AwayTeamId` (`string?`) - foreign key dedotta da policy SEO.

### Proprietà

- `Id` (`string`) - obbligatorietà non deducibile. Usata per lookup e URL SEO.
- `ChannelId` (`string?`) - opzionale deducibile da controllo `ChannelId != null`.
- `HomeTeamId` (`string?`) - opzionale/non deducibile. Controllata da policy SEO.
- `AwayTeamId` (`string?`) - opzionale/non deducibile. Controllata da policy SEO.
- `Status` (`MatchStatus`) - obbligatorietà non deducibile. Usata in filtri per stato e policy SEO.
- `DateUtc` (`DateTime`) - obbligatorietà non deducibile. Usata per filtri, ordinamento e `lastmod`.
- `Ts` (`DateTime`) - obbligatorietà non deducibile. Usata da `SeoUrlInventoryService` per calcolo `lastmod`.
- `IsDeleted` (`bool`) - obbligatorietà non deducibile. Usata come filtro applicativo.

### Relazioni

- `Channel` - Tipo relazione: navigation / foreign key. Evidenza: `ChannelId`, `Include(x => x.Channel)`, `match.Channel`.
- `Team` - Tipo relazione: navigation / foreign key. Evidenza: `HomeTeam`, `AwayTeam`, `HomeTeamId`, `AwayTeamId`.
- `MatchEvent` - Tipo relazione: collection navigation, cardinalità non deducibile. Evidenza: `Include(x => x.Events)` e `match.Events?.Count`.
- `Blog` - Tipo relazione: collection navigation, cardinalità non deducibile. Evidenza: `Include(x => x.Blogs)` e `match.Blogs?.Any(blog => blog.IsBlogStatic == true)`.

### Enum usati

- `MatchStatus`

### Validator associati

- `IValidator<Match>` iniettato nel costruttore di `MatchRepository`.
- Regole validator: non deducibili dai soli file modificati analizzati.

### Note di confidenza

- Parzialmente dedotto dai repository modificati e dal service SEO modificato.

## Team

### Nome wiki suggerito

`TeamEntity`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/TeamBox/Team.cs`

### Namespace

Non deducibile dai soli file modificati analizzati.

### Tipo

Entity persistente dedotta da `IRepository<Team>` e query EF in `TeamRepository`.

### Identificativo

- Proprietà chiave: `Id` (`string`) - dedotta da `GetByIdIncludeAsync(string teamId)` e URL SEO `/g-team/{team.Id}`.
- Altri identificativi:
  - `ChannelId` (`string?`) - foreign key dedotta da `FindByChannelAsync`.

### Proprietà

- `Id` (`string?`) - opzionale/non deducibile. Usata da lookup, filtro non null e URL SEO.
- `ChannelId` (`string?`) - opzionale deducibile da controllo `ChannelId != null`.
- `Name` (`string?`) - opzionale deducibile da controllo `!string.IsNullOrEmpty(x.Name)`.
- `IsDeleted` (`bool`) - obbligatorietà non deducibile. Usata come filtro applicativo.
- `Ts` (`DateTime`) - obbligatorietà non deducibile. Usata da `SeoUrlInventoryService` per `lastmod`.

### Relazioni

- `Channel` - Tipo relazione: navigation / foreign key. Evidenza: `ChannelId`, `Include(x => x.Channel)`, `team.Channel`.
- `User` - Tipo relazione: navigation transitiva tramite `Channel`. Evidenza: `ThenInclude(x => x!.User)`.

### Enum usati

- Nessun enum deducibile dai file modificati analizzati.

### Validator associati

- `IValidator<Team>` iniettato nel costruttore di `TeamRepository`.
- Regole validator: non deducibili dai soli file modificati analizzati.

### Note di confidenza

- Parzialmente dedotto dai repository modificati.

## Enum

## MatchStatus

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchStatus.cs`

### Valori

- `InProgress` - usato nei filtri repository.
- `Halftime` - usato nei filtri repository.
- `Scheduled` - usato nei filtri repository.
- `FullTime` - usato nei filtri repository e nella policy SEO.
- Altri valori: non deducibili dai soli file modificati analizzati.

### Usato da

- `Match.Status`

## Repository

## ChannelRepository

### Nome wiki suggerito

`ChannelRepository`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/ChannelBox/ChannelRepository.cs`
- `src/RugbyRadio/Lib/Repositories/ChannelBox/IChannelRepository.cs`

### Entity principali

- `Channel`

### Dipendenze dati

- `AppDbContext`
- `IValidator<Channel>`

### Metodi pubblici significativi

#### GetByIdAsync(string id)

- Input:
  - `id` (`string`)
- Output:
  - `Task<Channel?>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Channel`
- Filtri principali:
  - `x.Id == id`
  - `!x.IsDeleted`
- Include / join / navigazioni:
  - `Owners`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### CountByUserAsync(string userId)

- Input:
  - `userId` (`string`)
- Output:
  - `Task<int>`
- Operazione dati:
  - Aggregazione
- Entity coinvolte:
  - `Channel`
- Filtri principali:
  - `UserId != null`
  - `!IsDeleted`
  - `UserId == userId.ToUpper()` oppure owner con `UserId == userId.ToUpper()`
- Include / join / navigazioni:
  - `Owners`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### GetByPublicIdIncludeAsync(string publicId)

- Input:
  - `publicId` (`string`)
- Output:
  - `Task<Channel?>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Channel`
  - `User`
- Filtri principali:
  - `PublicId != null`
  - `PublicId == publicId.ToUpper()`
  - `!IsDeleted`
- Include / join / navigazioni:
  - `User`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### GetByPublicIdAsync(string publicId)

- Input:
  - `publicId` (`string`)
- Output:
  - `Task<Channel?>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Channel`
- Filtri principali:
  - `PublicId != null`
  - `PublicId == publicId.ToUpper()`
  - `!IsDeleted`
- Include / join / navigazioni:
  - Nessuna deducibile
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByUserAsync(string userId)

- Input:
  - `userId` (`string`)
- Output:
  - `Task<IList<Channel>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Channel`
- Filtri principali:
  - `UserId != null`
  - `!IsDeleted`
  - `UserId == userId.ToUpper()` oppure owner con `UserId == userId.ToUpper()`
- Include / join / navigazioni:
  - `Owners`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByFilterAsync(string? textToSearch, int page, int pageSize)

- Input:
  - `textToSearch` (`string?`)
  - `page` (`int`)
  - `pageSize` (`int`)
- Output:
  - `Task<IPagedList<Channel>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Channel`
  - `User`
  - `Match`
- Filtri principali:
  - esclude canali con `IsTestChannel == true`
  - ricerca su `Name`, `HeaderTitle`, `HeaderSubtitle`, `User.Nickname`
- Include / join / navigazioni:
  - `User`
  - `Matches`
- Ordinamento / paginazione:
  - ordina per data dell'ultimo match discendente
  - usa `PaginateAsync(page, pageSize)`
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindSeoCandidatesAsync()

- Input:
  - Nessuno
- Output:
  - `Task<IList<Channel>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Channel`
  - `Match`
- Filtri principali:
  - `!IsDeleted`
  - `!string.IsNullOrEmpty(PublicId)`
  - `IsTestChannel != true`
- Include / join / navigazioni:
  - `Matches`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

## MatchRepository

### Nome wiki suggerito

`MatchRepository`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchRepository.cs`
- `src/RugbyRadio/Lib/Repositories/MatchBox/IMatchRepository.cs`

### Entity principali

- `Match`

### Dipendenze dati

- `AppDbContext`
- `IValidator<Match>`

### Metodi pubblici significativi

#### CountByChannelAsync(string? channelId)

- Input:
  - `channelId` (`string?`)
- Output:
  - `Task<int>`
- Operazione dati:
  - Aggregazione
- Entity coinvolte:
  - `Match`
- Filtri principali:
  - se `channelId` vuoto ritorna `0`
  - `ChannelId != null`
  - `ChannelId == channelId.ToUpper()`
- Include / join / navigazioni:
  - Nessuna deducibile
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByChannelByIdIncludeAsync(string channelId)

- Input:
  - `channelId` (`string`)
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
- Filtri principali:
  - `ChannelId != null`
  - `ChannelId == channelId.ToUpper()`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### GetByIdIncludeAsync(string matchId)

- Input:
  - `matchId` (`string`)
- Output:
  - `Task<Match?>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
  - `Channel`
- Filtri principali:
  - `Id != null`
  - `Id == matchId.ToUpper()`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
  - `Channel`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByChannelAsync(string channelId, int page, int pageSize)

- Input:
  - `channelId` (`string`)
  - `page` (`int`)
  - `pageSize` (`int`)
- Output:
  - `Task<IPagedList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
- Filtri principali:
  - `ChannelId != null`
  - `ChannelId == channelId.ToUpper()`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
- Ordinamento / paginazione:
  - `PaginateAsync(page, pageSize, "Date", "DESC")`
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByFilterIncludeAsync(string? textToSearch, int? status, bool excludeTrainingMatches, int page, int pageSize)

- Input:
  - `textToSearch` (`string?`)
  - `status` (`int?`)
  - `excludeTrainingMatches` (`bool`)
  - `page` (`int`)
  - `pageSize` (`int`)
- Output:
  - `Task<IPagedList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
  - `Channel`
  - `User`
- Filtri principali:
  - se `excludeTrainingMatches` e true, include record con `Channel == null` o `Channel.IsTestChannel != true`
  - se `status == 10`, filtra stato richiesto e `DateUtc > DateTime.UtcNow.AddDays(-1)`
  - se `status == 20`, filtra `InProgress` o `Halftime`
  - altrimenti filtra su `(MatchStatus)status.Value`
  - ricerca testuale su team, canale e nickname utente con `EF.Functions.Like`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
  - `Channel`
  - `Channel.User`
- Ordinamento / paginazione:
  - `PaginateAsync(page, pageSize, "DateUtc", "DESC")`
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByFilterIncludeAsync(MatchStatus status, int page, int pageSize)

- Input:
  - `status` (`MatchStatus`)
  - `page` (`int`)
  - `pageSize` (`int`)
- Output:
  - `Task<IPagedList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
  - `MatchEvent`
  - `LineupPlayer`
  - `Player`
- Filtri principali:
  - `Status == status`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
  - `Events.LineupPlayer.Player`
- Ordinamento / paginazione:
  - `PaginateAsync(page, pageSize, "ID", "DESC")`
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindOnGoingIncludeByUserIdAsync(string userId)

- Input:
  - `userId` (`string`)
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Channel`
  - `User`
- Filtri principali:
  - partite con `Channel.User.Id == userId`
  - partite con owner canale `Owners.Any(o => o.UserId == userId)`
  - stato `InProgress`, `Halftime` o `Scheduled`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
  - `Channel.User`
  - `Channel.Owners`
- Ordinamento / paginazione:
  - `OrderBy(x => x.DateUtc)` per entrambe le query
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindWithoutBlogAsync(int languages)

- Input:
  - `languages` (`int`)
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Blog`
  - `Team`
  - `MatchEvent`
  - `LineupPlayer`
  - `Player`
- Filtri principali:
  - `Status == MatchStatus.FullTime`
  - `!IsDeleted`
  - `Blogs == null || Blogs.Count != languages`
- Include / join / navigazioni:
  - `Blogs`
  - `HomeTeam`
  - `AwayTeam`
  - `Events.LineupPlayer.Player`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindWithoutImageAsync()

- Input:
  - Nessuno
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
  - `Channel`
- Filtri principali:
  - `Status == MatchStatus.FullTime`
  - `!IsDeleted`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
  - `Channel`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindByDateAsync(DateTime startDate)

- Input:
  - `startDate` (`DateTime`)
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
- Filtri principali:
  - `DateUtc >= startDate`
- Include / join / navigazioni:
  - Nessuna deducibile
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindSeoCandidatesAsync()

- Input:
  - Nessuno
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Team`
  - `Channel`
  - `MatchEvent`
  - `Blog`
- Filtri principali:
  - `!IsDeleted`
  - `Status == MatchStatus.FullTime`
- Include / join / navigazioni:
  - `HomeTeam`
  - `AwayTeam`
  - `Channel`
  - `Events`
  - `Blogs`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindTrainingByUserIdAsync(string userId)

- Input:
  - `userId` (`string`)
- Output:
  - `Task<IList<Match>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Match`
  - `Channel`
- Filtri principali:
  - `Channel != null`
  - `Channel.UserId == userId`
  - `Channel.IsTestChannel == true`
- Include / join / navigazioni:
  - `Channel`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

## TeamRepository

### Nome wiki suggerito

`TeamRepository`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/TeamBox/TeamRepository.cs`
- `src/RugbyRadio/Lib/Repositories/TeamBox/ITeamRepository.cs`

### Entity principali

- `Team`

### Dipendenze dati

- `AppDbContext`
- `IValidator<Team>`

### Metodi pubblici significativi

#### FindByChannelAsync(string channelId)

- Input:
  - `channelId` (`string`)
- Output:
  - `Task<IList<Team>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Team`
- Filtri principali:
  - `ChannelId != null`
  - `ChannelId == channelId.ToUpper()`
  - `!IsDeleted`
- Include / join / navigazioni:
  - Nessuna deducibile
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### GetByIdIncludeAsync(string teamId)

- Input:
  - `teamId` (`string`)
- Output:
  - `Task<Team?>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Team`
  - `Channel`
  - `User`
- Filtri principali:
  - `Id != null`
  - `Id == teamId.ToUpper()`
  - `!IsDeleted`
- Include / join / navigazioni:
  - `Channel`
  - `Channel.User`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

#### FindSeoCandidatesAsync()

- Input:
  - Nessuno
- Output:
  - `Task<IList<Team>>`
- Operazione dati:
  - Lettura
- Entity coinvolte:
  - `Team`
  - `Channel`
- Filtri principali:
  - `!IsDeleted`
  - `Channel != null`
  - `Channel.IsDeleted == false`
  - `Channel.IsTestChannel != true`
  - `!string.IsNullOrEmpty(Name)`
- Include / join / navigazioni:
  - `Channel`
- Ordinamento / paginazione:
  - Nessuno deducibile
- Side effects dati:
  - Nessuno deducibile
- Note di confidenza:
  - Verificato dal codice

## Schemi dati non persistenti esclusi

## SeoPublicUrl

### File sorgente

- `src/RugbyRadio/HF/Dto/SeoPublicUrl.cs`

### Motivo esclusione

- Il commento sorgente dichiara che `SeoPublicUrl` non e una database entity.
- La classe e una rappresentazione interna per inventory SEO e sitemap, ricostruita da route frontend, repository e riferimenti sitemap blog.

### Proprietà rilevate

- `Loc` (`string`)
- `Type` (`SeoPublicUrlType`)
- `LastMod` (`DateTimeOffset?`)
- `IsIndexable` (`bool`)
- `CanonicalLoc` (`string?`)
- `Source` (`SeoPublicUrlSource`)
- `PriorityCandidate` (`decimal?`)
- `ChangeFrequency` (`SeoChangeFrequency?`)
- `SourceId` (`string?`)
- `ExclusionReason` (`string?`)
- `ShouldIncludeInSitemap` (`bool`, proprietà calcolata)
- `IsCanonical` (`bool`, proprietà calcolata)

### Enum non persistenti collegati

- `SeoPublicUrlType`
- `SeoPublicUrlSource`
- `SeoChangeFrequency`

## Note finali

- Limiti dell'analisi:
  - Il filtro richiesto limita la mappa ai soli file modificati dopo il 2026-05-14.
  - `AppDbContext` non risulta modificato nel periodo, quindi non sono stati censiti `DbSet` o configurazioni `OnModelCreating`.
  - I file sorgente completi delle entity non risultano modificati nel periodo e non sono stati usati per arricchire proprietà non visibili nei repository modificati.
- Elementi esclusi:
  - DTO non persistenti, helper, job, template, configurazioni e bootstrap applicativo.
  - Controller HTTP e routing API.
  - Service layer in dettaglio.
- Elementi non deducibili:
  - Obbligatorietà completa delle proprietà entity.
  - Configurazioni EF/Core, indici, chiavi composte, conversioni e seed.
  - Regole complete dei validator collegati.
  - Cardinalità completa delle relazioni.
- Conferma:
  - Nessun contenuto wiki e stato creato o aggiornato.
