# Backend Job Map RAW

## Sintesi

- Data analisi: 2026-05-19
- Root backend analizzata: `src/RugbyRadio`
- Filtro applicato: soli file modificati dopo il 2026-05-14
- File output: `llm-wiki/raw/backend/api/job-map-20260519.md`
- Job trovati: 2
- Trigger/schedulazioni trovati: 1
- Enqueue trovati: 0

## File analizzati

- `src/RugbyRadio/HF/Jobs/Core/BaseCoreJob.cs`
- `src/RugbyRadio/HF/Jobs/CreateMatchBlogHtmlJob.cs`
- `src/RugbyRadio/HF/Jobs/GenerateSeoSitemapJob.cs`
- `src/RugbyRadio/HF/Program.cs`
- `src/RugbyRadio/HF/Services/ISeoUrlInventoryService.cs`
- `src/RugbyRadio/HF/Services/SeoUrlInventoryService.cs`
- `src/RugbyRadio/HF/Helpers/SitemapXmlWriter.cs`
- `src/RugbyRadio/HF/Dto/SeoPublicUrl.cs`
- `src/RugbyRadio/HF/Dto/SeoPublicUrlRules.cs`
- `src/RugbyRadio/HF/appsettings.json`
- `src/RugbyRadio/Lib/Settings/SeoSitemapSettings.cs`

## File modificati esclusi perché non job

- `src/RugbyRadio/Api/Api.xml` - documentazione XML generata.
- `src/RugbyRadio/HF/Templates/post-template.html` - template HTML.
- `src/RugbyRadio/Lib/Repositories/ChannelBox/ChannelRepository.cs` - repository dati.
- `src/RugbyRadio/Lib/Repositories/ChannelBox/IChannelRepository.cs` - interfaccia repository.
- `src/RugbyRadio/Lib/Repositories/MatchBox/IMatchRepository.cs` - interfaccia repository.
- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchRepository.cs` - repository dati.
- `src/RugbyRadio/Lib/Repositories/TeamBox/ITeamRepository.cs` - interfaccia repository.
- `src/RugbyRadio/Lib/Repositories/TeamBox/TeamRepository.cs` - repository dati.

## Runtime / Registrazioni job

## HangfireRuntime

### File sorgente

- `src/RugbyRadio/HF/Program.cs`

### Registrazioni rilevate

- Tipo: Non deducibile
- Job/metodo: `Non deducibile`
- Cron/timing: `Non deducibile`
- Trigger: startup
- Evidenza:
  - `AddHangfire(...)`
  - `UseSqlServerStorage(builder.Configuration.GetConnectionString("HF"))`
  - `WithJobExpirationTimeout(TimeSpan.FromHours(12))`
  - `UseRecurringJobAdmin(Assembly.GetExecutingAssembly())`
  - `AddHangfireServer()`
  - `UseHangfireDashboard()`

### Note

- Nei file modificati dopo il 2026-05-14 non risultano chiamate a `RecurringJob.AddOrUpdate`, `BackgroundJob.Enqueue` o `BackgroundJob.Schedule`.
- La configurazione abilita runtime/server/dashboard Hangfire e discovery/admin dei recurring job, ma non espone nel codice modificato un cron specifico per i job censiti.

## Job

## CreateMatchBlogHtmlJob

### Nome wiki suggerito

`CreateMatchBlogHtmlJob`

### File sorgente

- `src/RugbyRadio/HF/Jobs/CreateMatchBlogHtmlJob.cs`

### Tipo

Non deducibile

### Responsabilità tecnica

Genera pagine HTML statiche del blog per match, indici paginati, landing root e sitemap blog, aggiornando lo stato dei blog staticizzati.

### Dipendenze iniettate

- `IMatchRepository`
- `IBlogRepository`
- `IUnitOfWork`
- `IOptions<BlogSettings>`

### Metodi eseguibili

#### ExecuteAsync()

- Input:
  - Nessuno
- Output:
  - `Task`
- Trigger deducibili:
  - Non deducibile
- Dipendenze chiamate:
  - `IBlogRepository.FindNotStaticAsync(...)`
  - `IMatchRepository.GetByIdIncludeAsync(...)`
  - `IBlogRepository.UpdateAsync(...)`
  - `IUnitOfWork.SaveChangesAsync()`
  - `IBlogRepository.FindAllStaticAsync(...)`
  - `IBlogRepository.FindLatestStaticAsync(...)`
- Entità coinvolte:
  - Lettura: `Blog`
  - Lettura: `Match`
  - Scrittura: `Blog`
- DTO / payload:
  - Nessuno deducibile
- Condizioni rilevanti:
  - Lingue processate: `IT`, `EN`, `FR`, `ES`, `JA`.
  - Se non ci sono blog non statici per lingua, scrive log e ritorna `false`.
  - Se `blog.MatchId` e vuoto, salta il blog.
  - Se il match non viene trovato, scrive log e continua.
  - Rigenera indice paginato per lingua solo se quella lingua ha prodotto almeno un post.
  - Rigenera landing root solo se almeno una lingua ha prodotto nuovi post.
- Side effects:
  - generazione file HTML.
  - generazione file XML sitemap blog.
  - cancellazione file HTML esistenti o legacy prima della riscrittura.
  - scrittura database: `blog.IsBlogStatic = true`, update blog e save changes.
- Retry / fallback:
  - `[AutomaticRetry(Attempts = 0)]`
  - fallback template a stringa vuota se il file template non esiste.
  - titolo troncato se supera la lunghezza massima prevista da `TruncateTitle`.
- Errori / casi limite:
  - early return se non ci sono blog da processare.
  - continue se `MatchId` e assente.
  - continue se il match collegato non esiste.
  - `GetBlogRootUrl()` usa `BlogSettings.BlogDomain.TrimEnd('/')`; gestione null/non valorizzato non deducibile da questo metodo.
- Note di confidenza:
  - Verificato dal codice

### Metodi interni rilevanti

#### CreateBlogHtml(string language)

- Input:
  - `language` (`string`)
- Output:
  - `Task<bool>`
- Trigger deducibili:
  - invocazione diretta da `ExecuteAsync()`
- Dipendenze chiamate:
  - `IBlogRepository.FindNotStaticAsync(...)`
  - `IMatchRepository.GetByIdIncludeAsync(...)`
  - `IBlogRepository.UpdateAsync(...)`
  - `IUnitOfWork.SaveChangesAsync()`
- Entità coinvolte:
  - Lettura: `Blog`, `Match`
  - Scrittura: `Blog`
- DTO / payload:
  - HTML statico generato da template.
- Condizioni rilevanti:
  - processa solo blog con `MatchId` e match esistente.
- Side effects:
  - scrittura file HTML dettaglio post.
  - aggiornamento `Blog.IsBlogStatic`.
- Retry / fallback:
  - fallback template vuoto se template mancante.
- Errori / casi limite:
  - early return se non ci sono blog.
  - skip blog senza match.
- Note di confidenza:
  - Verificato dal codice

#### GeneratePaginatedIndexAsync(string language)

- Input:
  - `language` (`string`)
- Output:
  - `Task`
- Trigger deducibili:
  - invocazione diretta da `ExecuteAsync()` quando sono stati processati nuovi post per lingua.
- Dipendenze chiamate:
  - `IBlogRepository.FindAllStaticAsync(...)`
- Entità coinvolte:
  - Lettura: `Blog`
- DTO / payload:
  - HTML indice paginato.
- Condizioni rilevanti:
  - `totalPages` minimo pari a `1`.
- Side effects:
  - scrittura file HTML `index.html` o `index-{n}.html`.
  - cancellazione file esistente prima della riscrittura.
- Retry / fallback:
  - fallback template vuoto se template mancante.
- Errori / casi limite:
  - Non deducibile
- Note di confidenza:
  - Verificato dal codice

#### GenerateLandingRootAsync()

- Input:
  - Nessuno
- Output:
  - `Task`
- Trigger deducibili:
  - invocazione diretta da `ExecuteAsync()` quando almeno una lingua ha prodotto nuovi post.
- Dipendenze chiamate:
  - `IBlogRepository.FindLatestStaticAsync(...)`
- Entità coinvolte:
  - Lettura: `Blog`
- DTO / payload:
  - HTML landing root.
- Condizioni rilevanti:
  - usa ultimi 9 blog statici.
- Side effects:
  - scrittura file `index.html` nel percorso blog configurato.
  - cancellazione file esistente prima della riscrittura.
- Retry / fallback:
  - fallback template vuoto se template mancante.
- Errori / casi limite:
  - Non deducibile
- Note di confidenza:
  - Verificato dal codice

#### GenerateSitemapAsync(string language)

- Input:
  - `language` (`string`)
- Output:
  - `Task`
- Trigger deducibili:
  - invocazione diretta da `CreateBlogHtml(string language)`.
- Dipendenze chiamate:
  - `IBlogRepository.FindAllStaticAsync(...)`
- Entità coinvolte:
  - Lettura: `Blog`
- DTO / payload:
  - XML sitemap per lingua.
- Condizioni rilevanti:
  - genera URL per tutti i blog statici della lingua.
- Side effects:
  - scrittura file `sitemap-{language}.xml`.
- Retry / fallback:
  - Non deducibile
- Errori / casi limite:
  - Non deducibile
- Note di confidenza:
  - Verificato dal codice

#### GenerateSitemapIndexAsync()

- Input:
  - Nessuno
- Output:
  - `Task`
- Trigger deducibili:
  - invocazione diretta da `ExecuteAsync()`.
- Dipendenze chiamate:
  - Nessuna dipendenza iniettata deducibile.
- Entità coinvolte:
  - Nessuna deducibile
- DTO / payload:
  - XML sitemap index.
- Condizioni rilevanti:
  - lingue incluse: `it`, `en`, `fr`, `es`, `ja`.
- Side effects:
  - scrittura file `sitemap-index.xml`.
- Retry / fallback:
  - Non deducibile
- Errori / casi limite:
  - Non deducibile
- Note di confidenza:
  - Verificato dal codice

## GenerateSeoSitemapJob

### Nome wiki suggerito

`GenerateSeoSitemapJob`

### File sorgente

- `src/RugbyRadio/HF/Jobs/GenerateSeoSitemapJob.cs`

### Tipo

Non deducibile

### Responsabilità tecnica

Genera sitemap XML pubbliche per URL statici, match, canali, squadre e sitemap index principale.

### Dipendenze iniettate

- `ISeoUrlInventoryService`
- `SitemapXmlWriter`
- `IOptions<SeoSitemapSettings>`
- `ILogger<GenerateSeoSitemapJob>`

### Metodi eseguibili

#### ExecuteAsync()

- Input:
  - Nessuno
- Output:
  - `Task`
- Trigger deducibili:
  - Non deducibile
- Dipendenze chiamate:
  - `ISeoUrlInventoryService.GetStaticUrls(...)`
  - `ISeoUrlInventoryService.GetMatchUrlsAsync()`
  - `ISeoUrlInventoryService.GetChannelUrlsAsync()`
  - `ISeoUrlInventoryService.GetTeamUrlsAsync()`
  - `ISeoUrlInventoryService.GetBlogSitemapReferenceAsync()`
  - `SitemapXmlWriter.WriteUrlSet(...)`
  - `SitemapXmlWriter.WriteSitemapIndex(...)`
  - `ILogger.LogInformation(...)`
- Entità coinvolte:
  - Lettura: non deducibile direttamente dal job.
  - Scrittura: nessuna entity deducibile.
- DTO / payload:
  - `SeoPublicUrl`
  - `SeoSitemapReference`
- Condizioni rilevanti:
  - scarta URL non includibili tramite `url.ShouldIncludeInSitemap`.
  - ordina URL per `Loc`.
  - usa data generazione come fallback per `lastmod`.
- Side effects:
  - generazione file sitemap XML.
  - creazione directory output.
  - scrittura file temporanei.
  - sostituzione atomica o overwrite dei file target.
  - log su logger e console Hangfire.
- Retry / fallback:
  - `[AutomaticRetry(Attempts = 0)]`
  - `[DisableConcurrentExecution(timeoutInSeconds: 3600)]`
  - fallback `StaticFilesPath` a `OutputPath` se non valorizzato.
  - fallback `lastmod` a data generazione se cartella o file statici non esistono.
  - fallback da `File.Replace` a `File.Move(..., overwrite: true)` per `IOException`, `UnauthorizedAccessException`, `PlatformNotSupportedException`.
- Errori / casi limite:
  - lancia `InvalidOperationException` se `SeoSitemap:OutputPath` non e valorizzato.
  - valida XML con `XDocument.Parse(xml)` prima della scrittura.
  - elimina file temporaneo nel blocco `finally` se ancora presente.
- Note di confidenza:
  - Verificato dal codice

### Metodi interni rilevanti

#### RunAsync()

- Input:
  - Nessuno
- Output:
  - `Task`
- Trigger deducibili:
  - invocazione diretta da `ExecuteAsync()`.
- Dipendenze chiamate:
  - `ISeoUrlInventoryService`
  - `SitemapXmlWriter`
  - `ILogger`
- Entità coinvolte:
  - Non deducibile direttamente dal job.
- DTO / payload:
  - `SeoPublicUrl`
  - `SeoSitemapReference`
- Condizioni rilevanti:
  - genera sitemap static, matches, channels, teams e root index.
- Side effects:
  - generazione file XML.
- Retry / fallback:
  - vedi `ExecuteAsync()`.
- Errori / casi limite:
  - vedi `ExecuteAsync()`.
- Note di confidenza:
  - Verificato dal codice

#### WriteXmlFileAtomicallyAsync(string outputPath, string fileName, string xml)

- Input:
  - `outputPath` (`string`)
  - `fileName` (`string`)
  - `xml` (`string`)
- Output:
  - `Task`
- Trigger deducibili:
  - invocazione interna da `WriteUrlSetAsync` e `WriteSitemapIndexAsync`.
- Dipendenze chiamate:
  - `XDocument.Parse(...)`
  - `File.WriteAllTextAsync(...)`
  - `File.Replace(...)`
  - `File.Move(...)`
- Entità coinvolte:
  - Nessuna
- DTO / payload:
  - XML sitemap.
- Condizioni rilevanti:
  - scrive prima su file temporaneo.
  - sostituisce o muove sul file target.
- Side effects:
  - scrittura filesystem.
  - cancellazione file temporaneo.
- Retry / fallback:
  - fallback a `File.Move(..., overwrite: true)` se replace fallisce per eccezioni gestite.
- Errori / casi limite:
  - XML non valido causa eccezione da `XDocument.Parse`.
- Note di confidenza:
  - Verificato dal codice

## Enqueue e trigger rilevati

## HangfireRuntimeStartup

### File sorgente

- `src/RugbyRadio/HF/Program.cs`

### Trigger

- Metodo chiamante: startup applicazione HF
- Job accodato: `Non deducibile`
- Parametri passati: `Non deducibile`
- Condizioni per accodamento: `Non deducibile`

### Note

- Nessuna chiamata `BackgroundJob.Enqueue`, `BackgroundJob.Schedule` o `RecurringJob.AddOrUpdate` trovata nei file modificati dopo il 2026-05-14.
- `UseRecurringJobAdmin(Assembly.GetExecutingAssembly())` abilita gestione/admin dei recurring job, ma non rende deducibile una schedulazione specifica.

## Note finali

- Limiti dell'analisi:
  - Il filtro richiesto limita la mappa ai soli file modificati dopo il 2026-05-14.
  - Altri job presenti nel repository ma non modificati nel periodo non sono stati censiti in dettaglio.
  - Trigger e cron specifici non sono deducibili dai file modificati.
- Elementi esclusi:
  - Repository database interni.
  - Service layer completo.
  - Controller HTTP.
  - Template HTML.
  - File generati/documentazione.
- Elementi non deducibili:
  - schedulazione cron dei due job censiti.
  - origine esatta dell'invocazione dei job.
  - parametri runtime Hangfire oltre quelli visibili in `Program.cs`.
  - eventuali recurring job configurati tramite dashboard o storage Hangfire.
- Conferma:
  - Nessun file in `llm-wiki/wiki/` e stato creato o modificato.
