# Backend Entities RAW

## Sintesi

- Data analisi: 2026-05-14
- Project: api
- Root backend analizzata: `src/RugbyRadio/Lib/Repositories/`
- File output: `llm-wiki/raw/backend/api/entities-20260514.md`
- Entity backend trovate: 20
- Enum trovati: 4
- Validator collegati trovati: 11
- Repository collegati trovati: 21
- Service collegati trovati: 6
- Elementi incerti o esclusi: 5

---

## Criteri usati

- Classe esposta da `DbSet<T>` in `AppDbContext`
- Classe che estende `EntityBase` (che implementa `IEntityBase`)
- Classe con attributo `[Table("...")]` e utilizzo in repository come modello persistente
- Presenza di validator FluentValidation dedicato associato alla classe

---

## Entity backend

---

## EntityBase

### Nome nel codice

`EntityBase`

### Nome wiki suggerito

`EntityBase (api)`

### File sorgente

- `src/RugbyRadio/Lib/Core/Entities/EntityBase.cs`

### Namespace

`Lib.Core.Entities`

### Progetto o modulo

`Lib`

### Tipo

Base entity

### Evidenza di persistenza

- Base class estesa da tutte le entity persistenti
- `Repository<T>` richiede `T : class, IEntityBase`

### Sintesi tecnica

Classe base comune a tutte le entity persistenti della piattaforma. Fornisce chiave primaria (`Id`), flag di soft-delete (`IsDeleted`) e timestamp di creazione/modifica (`Ts`). L'`Id` viene generato dal repository base come stringa da 20 caratteri (datetime UTC + GUID, uppercase, senza separatori) se non valorizzato all'inserimento.

### Identificativi

- `Id` (`string`) — chiave primaria; default `string.Empty`; generata dal repository; max 20 caratteri.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Default `string.Empty`. Max 20 caratteri (validator base).
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Flag di cancellazione logica. Default `false`. Impostato a `false` all'inserimento dal repository.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Timestamp creazione/ultima modifica. Aggiornato automaticamente dal repository ad ogni `InsertAsync` e `UpdateAsync`.

### Relazioni

Nessuna relazione persistente esplicita individuata.

### Enum usati

Nessuno.

### Validator associati

Ogni entity concreta ha un proprio validator FluentValidation. Regola base comune: `Id` NotNull o NotEmpty, MaxLength 20.

### Repository collegati

- `Repository<T>` — repository base generico che gestisce insert, update, delete e query di base per tutte le entity che estendono `EntityBase`.

### Service backend collegati

Non deducibile a livello di base entity.

### Note di confidenza

Verificato dal codice.

---

## User

### Nome nel codice

`User`

### Nome wiki suggerito

`User (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/UserBox/User.cs`

### Namespace

`Lib.Repositories.UserBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<User> Users` in `AppDbContext`
- `[Table("Users")]`
- `UserRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta l'account di un utente registrato sulla piattaforma. Supporta registrazione via email/password e via provider OAuth (Google). Contiene token di attivazione, preferenze di lingua e riferimento a canali e token notifiche push.

### Identificativi

- `Id` (`string`) — ereditato da `EntityBase`; chiave primaria; max 20 caratteri.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata da `EntityBase`.
- `Nickname` (`string?`) - Obbligatorietà: opzionale. Nome pubblico; troncato a 20 caratteri in fase di registrazione.
- `Email` (`string?`) - Obbligatorietà: opzionale. Email di iscrizione; salvata in lowercase.
- `Password` (`string?`) - Obbligatorietà: opzionale. Password utente; commentata come "criptata" ma hashing non verificabile nel layer entity.
- `AvatarUrl` (`string?`) - Obbligatorietà: opzionale. Path immagine profilo.
- `ActivationToken` (`string?`) - Obbligatorietà: opzionale. Token di attivazione account; generato come `Guid.NewGuid().ToString()` alla registrazione.
- `Language` (`string?`) - Obbligatorietà: opzionale. Lingua preferita.
- `CommentaryLanguage` (`string?`) - Obbligatorietà: opzionale. Lingua preferita per la telecronaca.
- `Timezone` (`string?`) - Obbligatorietà: opzionale. Timezone predefinito.
- `Provider` (`string?`) - Obbligatorietà: opzionale. Provider OAuth (es. `"Google"`).
- `ProviderUserId` (`string?`) - Obbligatorietà: opzionale. Identificativo utente presso il provider.
- `IsVerified` (`bool`) - Obbligatorietà: obbligatoria. `true` se l'utente ha verificato l'email.
- `IsActive` (`bool`) - Obbligatorietà: obbligatoria. `true` se l'utente è attivo.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata. Soft-delete.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata. Timestamp.

### Relazioni

- `UserToken` — Tipo relazione: uno-a-molti. Evidenza: navigation property `NotificationTokens` (`IList<UserToken>?`), `[ResetNavigationProperty]`.
- `Channel` — Tipo relazione: uno-a-molti (canali di cui è owner primario). Evidenza: navigation property `Channels` (`IList<Channel>?`), `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato nel codice letto. Regole base da `EntityBase`.

### Repository collegati

- `UserRepository` — persiste e interroga `User` (lookup per email, password, token).

### Service backend collegati

- `UserService` — gestisce registrazione, login, attivazione, aggiornamento profilo, cancellazione, OTP.

### Note di confidenza

Verificato dal codice.

### Note

Il campo `Password` è annotato come "criptata" ma il meccanismo di hashing non è visibile nel layer entity/service letto.

---

## Channel

### Nome nel codice

`Channel`

### Nome wiki suggerito

`Channel (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/ChannelBox/Channel.cs`

### Namespace

`Lib.Repositories.ChannelBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente / Aggregate root (deducibile: ha metodo di dominio `IsOwner` e contiene le sue navigation verso co-owner e partite)

### Evidenza di persistenza

- `DbSet<Channel> Channels` in `AppDbContext`
- `[Table("Channels")]`
- `ChannelRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un canale di telecronaca creato da un utente. Contiene owner primario, co-owner, dati di personalizzazione (layout, immagine), flag di canale di test e partite associate. Espone il metodo `IsOwner(userId)` usato nei controlli di accesso applicativi.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.
- `PublicId` (`string?`) — identificativo pubblico per URL pubbliche; usato nelle API pubbliche.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `PublicId` (`string?`) - Obbligatorietà: opzionale. Identificativo pubblico per URL esposte.
- `UserId` (`string?`) - Obbligatorietà: opzionale. FK verso `User` (owner primario).
- `Name` (`string?`) - Obbligatorietà: opzionale. Nome pubblico del canale.
- `HeaderBgColor` (`string?`) - Obbligatorietà: opzionale. Colore sfondo intestazione.
- `HeaderFtColor` (`string?`) - Obbligatorietà: opzionale. Colore testo intestazione.
- `HeaderTitle` (`string?`) - Obbligatorietà: opzionale. Titolo intestazione.
- `HeaderSubtitle` (`string?`) - Obbligatorietà: opzionale. Sottotitolo intestazione.
- `ImageUrl` (`string?`) - Obbligatorietà: opzionale. URL immagine del canale.
- `IsTestChannel` (`bool?`) - Obbligatorietà: opzionale. `true` se canale di test/training.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata. Soft-delete.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata. Timestamp.

### Relazioni

- `User` — Tipo relazione: molti-a-uno (owner primario). Evidenza: navigation property `User` (`User?`), FK `UserId`, `[ResetNavigationProperty]`.
- `ChannelUser` — Tipo relazione: uno-a-molti (co-owner). Evidenza: navigation property `Owners` (`IList<ChannelUser>?`), `[ResetNavigationProperty]`.
- `Match` — Tipo relazione: uno-a-molti. Evidenza: navigation property `Matches` (`IList<Match>?`), `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato nel codice letto.

### Repository collegati

- `ChannelRepository` — persiste e interroga `Channel`.

### Service backend collegati

- `ChannelService` — gestisce creazione, aggiornamento, ricerca canali e verifica ownership.

### Note di confidenza

Verificato dal codice.

---

## ChannelUser

### Nome nel codice

`ChannelUser`

### Nome wiki suggerito

`ChannelUser (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/ChannelUserBox/ChannelUser.cs`

### Namespace

`Lib.Repositories.ChannelUserBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente (tabella di associazione co-owner/canale)

### Evidenza di persistenza

- `[Table("ChannelUsers")]`
- Estende `EntityBase`
- Referenziata come navigation property `Channel.Owners` e interrogata da `ChannelRepository` (include `Owners`)
- Non esposta da DbSet diretto ma mappata da EF Core via navigation

### Sintesi tecnica

Rappresenta l'associazione tra un utente co-owner e un canale. Usata per gestire la lista di co-editor di un canale. Il metodo `Channel.IsOwner(userId)` la interroga per verificare l'accesso.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `ChannelId` (`string?`) - Obbligatorietà: opzionale. FK verso `Channel`.
- `UserId` (`string?`) - Obbligatorietà: opzionale. FK verso `User` (co-owner).
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Channel` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Channel` (`Channel?`), FK `ChannelId`, `[ResetNavigationProperty]`.
- `User` — Tipo relazione: molti-a-uno. Evidenza: navigation property `User` (`User?`), FK `UserId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `ChannelRepository` — include `Owners` nelle query (accesso indiretto).
- `ChannelUserRepository` — persiste `ChannelUser` (dettaglio non letto).

### Service backend collegati

- `ChannelService` — aggiunge e rimuove co-owner tramite `ChannelUserRepository`.

### Note di confidenza

Verificato dal codice.

---

## Team

### Nome nel codice

`Team`

### Nome wiki suggerito

`Team (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/TeamBox/Team.cs`

### Namespace

`Lib.Repositories.TeamBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<Team> Teams` in `AppDbContext`
- `[Table("Teams")]`
- `TeamRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta una squadra di rugby associata a un canale. Contiene nome, logo, soprannome e FK verso il canale di appartenenza. Usata come `HomeTeam` e `AwayTeam` nelle partite.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `ChannelId` (`string?`) - Obbligatorietà: opzionale. FK verso `Channel`.
- `Name` (`string?`) - Obbligatorietà: opzionale. Nome della squadra.
- `LogoUrl` (`string?`) - Obbligatorietà: opzionale. URL del logo.
- `Nickname` (`string?`) - Obbligatorietà: opzionale. Soprannome della squadra.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Channel` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Channel` (`Channel?`), FK `ChannelId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `TeamRepository` — persiste e interroga `Team`.

### Service backend collegati

- `TeamService` — gestisce creazione, aggiornamento e ricerca squadre.

### Note di confidenza

Verificato dal codice.

---

## Player

### Nome nel codice

`Player`

### Nome wiki suggerito

`Player (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/PlayerBox/Player.cs`

### Namespace

`Lib.Repositories.PlayerBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<Player> Players` in `AppDbContext`
- `[Table("Players")]`
- `PlayerRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un giocatore anagrafico associato a una squadra. Contiene nome, soprannome, avatar e numero di maglia di default. Viene associato alle formazioni tramite `LineupPlayer`.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `TeamId` (`string?`) - Obbligatorietà: opzionale. FK verso `Team`.
- `Name` (`string?`) - Obbligatorietà: opzionale. Nome del giocatore.
- `Nickname` (`string?`) - Obbligatorietà: opzionale. Soprannome.
- `AvatarUrl` (`string?`) - Obbligatorietà: opzionale. URL avatar.
- `DefaultNumber` (`int?`) - Obbligatorietà: opzionale. Numero di maglia di default in formazione.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Team` — Tipo relazione: molti-a-uno (via FK `TeamId`). Evidenza: FK `TeamId` presente sull'entity; nessuna navigation property esplicita su `Player`, ma navigation inversa presente su `LineupPlayer.Player`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `PlayerRepository` — persiste e interroga `Player`.

### Service backend collegati

- `PlayerService` — gestisce creazione, aggiornamento ed eliminazione giocatori.

### Note di confidenza

Verificato dal codice.

---

## Match

### Nome nel codice

`Match`

### Nome wiki suggerito

`Match (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/Match.cs`

### Namespace

`Lib.Repositories.MatchBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente / Aggregate root (contiene navigation verso eventi, commenti, formazioni e blog)

### Evidenza di persistenza

- `DbSet<Match> Matches` in `AppDbContext`
- `[Table("Matches")]`
- `MatchRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta una partita di rugby telecronacata. Contiene dati anagrafici (canale, squadre, data, stadio), statistiche aggregate (punteggio, possesso, zone campo), stato della partita e flag di archiviazione. È l'aggregate root del contesto di telecronaca.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `ChannelId` (`string?`) - Obbligatorietà: opzionale. FK verso `Channel`.
- `HomeTeamId` (`string?`) - Obbligatorietà: opzionale. FK verso `Team` (squadra casa).
- `AwayTeamId` (`string?`) - Obbligatorietà: opzionale. FK verso `Team` (squadra ospite).
- `StadiumId` (`string?`) - Obbligatorietà: opzionale. Riferimento stadio; persistenza come entity separata non verificabile.
- `Timezone` (`string?`) - Obbligatorietà: opzionale. Timezone della partita.
- `ImageUrl` (`string?`) - Obbligatorietà: opzionale. URL immagine partita.
- `Gender` (`string?`) - Obbligatorietà: opzionale. Tipo partita (es. `"M"`, `"F"`).
- `Date` (`DateTime`) - Obbligatorietà: obbligatoria. Data e ora locale della partita.
- `DateUtc` (`DateTime`) - Obbligatorietà: obbligatoria. Data e ora UTC della partita.
- `Status` (`MatchStatus?`) - Obbligatorietà: opzionale. Stato della partita; salvato come `int` in DB.
- `HomeScore` (`int`) - Obbligatorietà: obbligatoria. Punteggio squadra casa. Default `0`.
- `AwayScore` (`int`) - Obbligatorietà: obbligatoria. Punteggio squadra ospite. Default `0`.
- `Minute` (`int`) - Obbligatorietà: obbligatoria. Minuto corrente della partita.
- `HalfMinutes` (`int`) - Obbligatorietà: obbligatoria. Durata di ogni tempo in minuti.
- `HomePossession` (`int`) - Obbligatorietà: obbligatoria. Somma eventi possesso casa.
- `AwayPossession` (`int`) - Obbligatorietà: obbligatoria. Somma eventi possesso ospite.
- `Zone1` (`int`) - Obbligatorietà: obbligatoria. Somma eventi zona 1.
- `Zone2` (`int`) - Obbligatorietà: obbligatoria. Somma eventi zona 2.
- `Zone3` (`int`) - Obbligatorietà: obbligatoria. Somma eventi zona 3.
- `Zone4` (`int`) - Obbligatorietà: obbligatoria. Somma eventi zona 4.
- `IsArchived` (`bool?`) - Obbligatorietà: opzionale. `true` se la partita è archiviata e non modificabile.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Channel` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Channel` (`Channel?`), FK `ChannelId`, `[ResetNavigationProperty]`.
- `Team` (HomeTeam) — Tipo relazione: molti-a-uno. Evidenza: navigation property `HomeTeam` (`Team?`), FK `HomeTeamId`, `[ResetNavigationProperty]`.
- `Team` (AwayTeam) — Tipo relazione: molti-a-uno. Evidenza: navigation property `AwayTeam` (`Team?`), FK `AwayTeamId`, `[ResetNavigationProperty]`.
- `Comment` — Tipo relazione: uno-a-molti. Evidenza: navigation property `Comments` (`IList<Comment>?`), `[ResetNavigationProperty]`.
- `MatchEvent` — Tipo relazione: uno-a-molti. Evidenza: navigation property `Events` (`IList<MatchEvent>?`), `[ResetNavigationProperty]`.
- `LineupPlayer` — Tipo relazione: uno-a-molti. Evidenza: navigation property `LineupPlayers` (`IList<LineupPlayer>?`), `[ResetNavigationProperty]`.
- `Blog` — Tipo relazione: uno-a-molti. Evidenza: navigation property `Blogs` (`IList<Blog>?`), `[ResetNavigationProperty]`.

### Enum usati

- `MatchStatus` — usato da `Match.Status`. Conversione `HasConversion<int>()` in `AppDbContext`.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `MatchRepository` — persiste e interroga `Match` con numerosi filtri e include.

### Service backend collegati

- `MatchService` — gestisce creazione, aggiornamento, eventi, stato, statistiche e logiche di accesso.

### Note di confidenza

Verificato dal codice.

---

## MatchEvent

### Nome nel codice

`MatchEvent`

### Nome wiki suggerito

`MatchEvent (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchEventBox/MatchEvent.cs`

### Namespace

`Lib.Repositories.MatchEventBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<MatchEvent> MatchEvents` in `AppDbContext`
- `[Table("Events")]`
- `MatchEventRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un singolo evento di telecronaca all'interno di una partita (meta, punizione, calcio d'inizio, segnali arbitro, ecc.). Contiene tipo evento, minuto, zona campo, riferimento ai giocatori coinvolti e commento libero.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `MatchId` (`string?`) - Obbligatorietà: opzionale. FK verso `Match`.
- `TeamId` (`string?`) - Obbligatorietà: opzionale. FK verso `Team` (squadra dell'evento).
- `LineupPlayerId` (`string?`) - Obbligatorietà: opzionale. FK verso `LineupPlayer` (giocatore principale).
- `LineupPlayer2Id` (`string?`) - Obbligatorietà: opzionale. FK verso `LineupPlayer` (secondo giocatore).
- `Territory` (`TerritoryType?`) - Obbligatorietà: opzionale. Zona del campo; salvata come `int` in DB.
- `Type` (`MatchEventType`) - Obbligatorietà: obbligatoria. Tipo evento; salvato come `int` in DB.
- `Minute` (`int`) - Obbligatorietà: obbligatoria. Minuto dell'evento.
- `TypeVersion` (`int`) - Obbligatorietà: obbligatoria. Versione del messaggio da usare per l'evento.
- `Comment` (`string?`) - Obbligatorietà: opzionale. Commento libero dell'autore.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Match` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Match` (`Match?`), FK `MatchId`, `[ResetNavigationProperty]`.
- `LineupPlayer` — Tipo relazione: molti-a-uno (giocatore principale). Evidenza: navigation property `LineupPlayer` (`LineupPlayer?`), FK `LineupPlayerId`, `[ResetNavigationProperty]`.

### Enum usati

- `MatchEventType` — usato da `MatchEvent.Type`. Conversione `HasConversion<int>()` in `AppDbContext`.
- `TerritoryType` — usato da `MatchEvent.Territory`. Conversione `HasConversion<int>()` in `AppDbContext`.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `MatchEventRepository` — persiste e interroga `MatchEvent`.

### Service backend collegati

- `MatchService` — aggiunge ed elimina eventi tramite `MatchEventRepository`.

### Note di confidenza

Verificato dal codice.

---

## LineupPlayer

### Nome nel codice

`LineupPlayer`

### Nome wiki suggerito

`LineupPlayer (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/LineupPlayerBox/LineupPlayer.cs`

### Namespace

`Lib.Repositories.LineupPlayerBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<LineupPlayer> LineupPlayers` in `AppDbContext`
- `[Table("LineupPlayers")]`
- `LineupPlayerRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta la posizione di un giocatore in formazione per una data partita. Collega `Match`, `Team` e `Player` con un numero di ruolo. Viene referenziata dagli eventi di partita per indicare i giocatori coinvolti.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `MatchId` (`string?`) - Obbligatorietà: opzionale. FK verso `Match`.
- `TeamId` (`string?`) - Obbligatorietà: opzionale. FK verso `Team`.
- `PlayerId` (`string?`) - Obbligatorietà: opzionale. FK verso `Player`.
- `Number` (`int`) - Obbligatorietà: obbligatoria. Numero di ruolo in formazione.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Player` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Player` (`Player?`), FK `PlayerId`, `[ResetNavigationProperty]`.
- `Match` — Tipo relazione: molti-a-uno (via FK `MatchId`). Evidenza: FK `MatchId`; navigation inversa `Match.LineupPlayers`.
- `Team` — Tipo relazione: molti-a-uno (via FK `TeamId`). Evidenza: FK `TeamId`; nessuna navigation property diretta su `LineupPlayer`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `LineupPlayerRepository` — persiste e interroga `LineupPlayer`.

### Service backend collegati

- `LineupPlayerService` — crea formazioni per le partite.
- `MatchService` — aggiorna e rimuove ruoli in formazione.

### Note di confidenza

Verificato dal codice.

---

## Comment

### Nome nel codice

`Comment`

### Nome wiki suggerito

`Comment (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/CommentBox/Comment.cs`

### Namespace

`Lib.Repositories.CommentBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<Comment> Comments` in `AppDbContext`
- `[Table("Comments")]`
- `CommentRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un commento lasciato da un utente su un evento di una partita. Contiene riferimento alla partita, all'evento specifico, all'utente autore e al testo del commento.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `MatchId` (`string?`) - Obbligatorietà: opzionale. FK verso `Match`.
- `EventId` (`string?`) - Obbligatorietà: opzionale. FK verso `MatchEvent`.
- `UserId` (`string?`) - Obbligatorietà: opzionale. FK verso `User`.
- `Message` (`string?`) - Obbligatorietà: opzionale. Testo del commento.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `User` — Tipo relazione: molti-a-uno. Evidenza: navigation property `User` (`User?`), FK `UserId`, `[ResetNavigationProperty]`.
- `Match` — Tipo relazione: molti-a-uno (via FK `MatchId`). Evidenza: FK `MatchId`; navigation inversa `Match.Comments`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `CommentRepository` — persiste e interroga `Comment`.

### Service backend collegati

- `MatchService` — aggiunge ed elimina commenti tramite `CommentRepository`.

### Note di confidenza

Verificato dal codice.

---

## EventReaction

### Nome nel codice

`EventReaction`

### Nome wiki suggerito

`EventReaction (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/EventReactionBox/EventReaction.cs`

### Namespace

`Lib.Repositories.EventReactionBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<EventReaction> EventReactions` in `AppDbContext`
- `[Table("EventReactions")]`
- `EventReactionRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta la reazione di un utente a un evento di partita. Contiene riferimento all'evento, all'utente e al tipo di reazione (stringa libera).

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `EventId` (`string?`) - Obbligatorietà: opzionale. FK verso `MatchEvent`.
- `UserId` (`string?`) - Obbligatorietà: opzionale. FK verso `User`.
- `ReactionType` (`string?`) - Obbligatorietà: opzionale. Tipo di reazione (stringa libera; valori ammessi non deducibili dal codice entity).
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `MatchEvent` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Event` (`MatchEvent?`), FK `EventId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

Nessun validator dedicato individuato.

### Repository collegati

- `EventReactionRepository` — persiste e interroga `EventReaction`.

### Service backend collegati

- `MatchService` — aggiunge reazioni tramite `EventReactionRepository`.

### Note di confidenza

Verificato dal codice.

---

## Subscription

### Nome nel codice

`Subscription`

### Nome wiki suggerito

`Subscription (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/SubscriptionBox/Subscription.cs`

### Namespace

`Lib.Repositories.SubscriptionBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<Subscription> Subscriptions` in `AppDbContext`
- `[Table("Subscriptions")]`
- `SubscriptionRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta la sottoscrizione di un utente a un canale o a una partita. Un record può riferirsi a un canale (follow canale) oppure a una partita (follow partita per notifiche). I due casi sono distinti tramite i FK `ChannelId` / `MatchId`.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria; NotNull, MaxLength 20 (da validator).

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata. MaxLength 20.
- `UserId` (`string?`) - Obbligatorietà: obbligatoria (NotNull da validator, MaxLength 20). FK verso `User`.
- `ChannelId` (`string?`) - Obbligatorietà: opzionale (MaxLength 20 da validator). FK verso `Channel`. Valorizzato per follow canale.
- `MatchId` (`string?`) - Obbligatorietà: opzionale. FK verso `Match`. Valorizzato per follow partita.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Channel` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Channel` (`Channel?`), FK `ChannelId`, `[ResetNavigationProperty]`.
- `Match` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Match` (`Match?`), FK `MatchId`, `[ResetNavigationProperty]`.
- `User` — Tipo relazione: molti-a-uno. Evidenza: navigation property `User` (`User?`), FK `UserId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

`SubscriptionValidator` (`src/RugbyRadio/Lib/Repositories/SubscriptionBox/SubscriptionValidator.cs`) — Regole: `Id` NotNull MaxLength 20; `UserId` NotNull MaxLength 20; `ChannelId` MaxLength 20.

### Repository collegati

- `SubscriptionRepository` — persiste e interroga `Subscription`.

### Service backend collegati

- `ChannelService` — gestisce follow canale tramite `SubscriptionRepository`.
- `MatchService` — gestisce follow partita tramite `SubscriptionRepository`.

### Note di confidenza

Verificato dal codice.

---

## LineupPlayerRate

### Nome nel codice

`LineupPlayerRate`

### Nome wiki suggerito

`LineupPlayerRate (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/LineupPlayerRateBox/LineupPlayerRate.cs`

### Namespace

`Lib.Repositories.LineupPlayerRateBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<LineupPlayerRate> LineupPlayerRates` in `AppDbContext`
- `[Table("LineupPlayerRates")]`
- `LineupPlayerRateRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta il voto assegnato da un utente a un giocatore in formazione per una partita. Permette di tracciare chi ha votato quale giocatore.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `LineupPlayerId` (`string?`) - Obbligatorietà: opzionale. FK verso `LineupPlayer`.
- `UserId` (`string?`) - Obbligatorietà: opzionale. FK verso `User` (utente che ha votato).
- `Rate` (`int`) - Obbligatorietà: obbligatoria (NotNull da validator). Voto assegnato.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `LineupPlayer` — Tipo relazione: molti-a-uno. Evidenza: navigation property `LineupPlayer` (`LineupPlayer?`), FK `LineupPlayerId`, `[ResetNavigationProperty]`.
- `User` — Tipo relazione: molti-a-uno. Evidenza: navigation property `User` (`User?`), FK `UserId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

`LineupPlayerRateValidator` (`src/RugbyRadio/Lib/Repositories/LineupPlayerRateBox/LineupPlayerRateValidator.cs`) — Regole: `Id` NotNull MaxLength 20 (ripetuto tre volte nel codice — probabile errore copy-paste); `Rate` NotNull.

### Repository collegati

- `LineupPlayerRateRepository` — persiste e interroga `LineupPlayerRate`.

### Service backend collegati

- `MatchService` — salva le votazioni giocatore tramite `LineupPlayerRateRepository`.

### Note di confidenza

Verificato dal codice.

### Note

Il validator contiene una regola su `Id` duplicata tre volte, probabilmente errore di copia nel codice sorgente.

---

## MatchLike

### Nome nel codice

`MatchLike`

### Nome wiki suggerito

`MatchLike (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchLikeBox/MatchLike.cs`

### Namespace

`Lib.Repositories.MatchLikeBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<MatchLike> MatchLikes` in `AppDbContext`
- Estende `EntityBase`
- `MatchLikeRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta il like assegnato da un utente a una partita. Tabella senza attributo `[Table]` esplicito nel codice letto.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata. MaxLength 20 (da validator).
- `MatchId` (`string?`) - Obbligatorietà: obbligatoria (NotNull MaxLength 20 da validator). FK verso `Match`.
- `UserId` (`string?`) - Obbligatorietà: obbligatoria (NotNull MaxLength 20 da validator). FK verso `User`.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Match` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Match` (`Match?`), FK `MatchId`, `[ResetNavigationProperty]`.
- `User` — Tipo relazione: molti-a-uno. Evidenza: navigation property `User` (`User?`), FK `UserId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

`MatchLikeValidator` (`src/RugbyRadio/Lib/Repositories/MatchLikeBox/MatchLikeValidator.cs`) — Regole: `Id` NotNull MaxLength 20; `MatchId` NotNull MaxLength 20; `UserId` NotNull MaxLength 20.

### Repository collegati

- `MatchLikeRepository` — persiste e interroga `MatchLike`.

### Service backend collegati

- `MatchService` — salva like partita tramite `MatchLikeRepository`.

### Note di confidenza

Verificato dal codice.

### Note

Attributo `[Table]` non presente nel codice letto. Il nome tabella DB non è deducibile con certezza (probabile `MatchLikes` per convenzione EF Core).

---

## UserToken

### Nome nel codice

`UserToken`

### Nome wiki suggerito

`UserToken (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/UserTokens/UserToken.cs`

### Namespace

`Lib.Repositories.UserTokens`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<UserToken> UserTokens` in `AppDbContext`
- `[Table("UserTokens")]`
- `UserTokenRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un token per le notifiche push (es. FCM) associato a un utente. Un utente può avere più token (dispositivi multipli). Referenziata da `User.NotificationTokens` e usata da `SubscriptionRepository` per l'invio notifiche.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata. MaxLength 20 (da validator).
- `UserId` (`string?`) - Obbligatorietà: obbligatoria (NotNull MaxLength 20 da validator). FK verso `User`.
- `Token` (`string?`) - Obbligatorietà: obbligatoria (NotNull da validator). Token notifica push.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `User` — Tipo relazione: molti-a-uno (via FK `UserId`). Evidenza: FK `UserId`; navigation inversa `User.NotificationTokens`.

### Enum usati

Nessuno.

### Validator associati

`UserTokenValidator` (`src/RugbyRadio/Lib/Repositories/UserTokens/UserTokenValidator.cs`) — Regole: `Id` NotNull MaxLength 20; `UserId` NotNull MaxLength 20; `Token` NotNull.

### Repository collegati

- `UserTokenRepository` — persiste e interroga `UserToken`.

### Service backend collegati

- `UserService` — salva token notifica tramite `UserTokenRepository`.

### Note di confidenza

Verificato dal codice.

---

## Blog

### Nome nel codice

`Blog`

### Nome wiki suggerito

`Blog (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/BlogBox/Blog.cs`

### Namespace

`Lib.Repositories.BlogBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<Blog> Blog` in `AppDbContext`
- `[Table("Blog")]`
- `BlogRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un post di blog generato per una partita terminata. Ogni partita può avere più post (uno per lingua). Contiene titolo, corpo, lingua e flag di stato (staticizzato, pubblicato su Facebook).

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata. MaxLength 20 (da validator).
- `MatchId` (`string?`) - Obbligatorietà: obbligatoria (NotNull MaxLength 20 da validator). FK verso `Match`.
- `Language` (`string?`) - Obbligatorietà: obbligatoria (NotNull MaxLength 2 da validator). Lingua del post (codice ISO 2 caratteri).
- `Title` (`string?`) - Obbligatorietà: obbligatoria (NotNull MaxLength 100 da validator). Titolo.
- `Body` (`string?`) - Obbligatorietà: obbligatoria (NotNull da validator). Contenuto del post.
- `IsBlogStatic` (`bool?`) - Obbligatorietà: opzionale. `true` se il blog è stato staticizzato.
- `IsFacebookPosted` (`bool?`) - Obbligatorietà: opzionale. `true` se il post è stato pubblicato su Facebook.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

- `Match` — Tipo relazione: molti-a-uno. Evidenza: navigation property `Match` (`Match?`), FK `MatchId`, `[ResetNavigationProperty]`.

### Enum usati

Nessuno.

### Validator associati

`BlogValidator` (`src/RugbyRadio/Lib/Repositories/BlogBox/BlogValidator.cs`) — Regole: `Id` NotNull MaxLength 20; `MatchId` NotNull MaxLength 20; `Language` NotNull MaxLength 2; `Title` NotNull MaxLength 100; `Body` NotNull.

### Repository collegati

- `BlogRepository` — persiste e interroga `Blog`.

### Service backend collegati

Non deducibili direttamente dalla lettura dei controller/service API.

### Note di confidenza

Verificato dal codice.

---

## UserOtpCode

### Nome nel codice

`UserOtpCode`

### Nome wiki suggerito

`UserOtpCode (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/UserOtpCodeBox/UserOtpCode.cs`

### Namespace

`Lib.Repositories.UserOtpCodeBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<UserOtpCode> UserOptCodes` in `AppDbContext` (nota: nome DbSet con typo `OptCodes`)
- `[Table("UserOtpCodes")]`
- `UserOtpCodeRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un codice OTP temporaneo generato per il reset password. Contiene il codice numerico a 6 cifre, la scadenza (1 ora) e un flag di utilizzo.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria; NotNull MaxLength 20 (da validator).

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata. MaxLength 20.
- `UserId` (`string`) - Obbligatorietà: obbligatoria. Non nullable; default `string.Empty`. FK verso `User`. MaxLength 20 (da validator).
- `OtpCode` (`int`) - Obbligatorietà: obbligatoria. Codice OTP numerico a 6 cifre (generato con `Random().Next(100000, 999999)`). NotNull (da validator).
- `Expiration` (`DateTime`) - Obbligatorietà: obbligatoria. Data/ora scadenza; impostata a `+1 ora` dalla generazione. NotNull (da validator).
- `IsUsed` (`bool`) - Obbligatorietà: obbligatoria. `true` se il codice è già stato utilizzato; impostato a `true` dopo il cambio password.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

Nessuna relazione persistente esplicita individuata (nessuna navigation property sull'entity).

### Enum usati

Nessuno.

### Validator associati

`UserOtpCodeValidator` (`src/RugbyRadio/Lib/Repositories/UserOtpCodeBox/UserOtpCodeValidator.cs`) — Regole: `Id` NotNull MaxLength 20; `UserId` NotNull MaxLength 20; `OtpCode` NotNull; `Expiration` NotNull.

### Repository collegati

- `UserOtpCodeRepository` — persiste e interroga `UserOtpCode`.

### Service backend collegati

- `UserService` — crea OTP (`ForgotPassword`) e lo verifica/invalida (`ChangePassword`).

### Note di confidenza

Verificato dal codice.

### Note

Il nome del `DbSet` in `AppDbContext` è `UserOptCodes` (typo: `Opt` invece di `Otp`).

---

## Email

### Nome nel codice

`Email`

### Nome wiki suggerito

`Email (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/EmailBox/Email.cs`

### Namespace

`Lib.Repositories.EmailBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<Email> Emails` in `AppDbContext`
- `[Table("Emails")]`
- `EmailRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta un'email da inviare o già inviata. Contiene destinatario (userId), oggetto, corpo, stato di invio e contatore tentativi. Usata dal sistema di invio email asincrono o in coda.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria; NotEmpty MaxLength 20 (da validator).

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata. MaxLength 20.
- `UserId` (`string`) - Obbligatorietà: obbligatoria. Non nullable; default `string.Empty`. FK verso `User`. NotEmpty MaxLength 20 (da validator).
- `Subject` (`string`) - Obbligatorietà: obbligatoria. Non nullable. Oggetto email. MaxLength 100 (da validator).
- `Body` (`string`) - Obbligatorietà: obbligatoria. Non nullable. Corpo email. NotEmpty (da validator).
- `Status` (`EmailStatus`) - Obbligatorietà: obbligatoria. Stato email; IsInEnum (da validator).
- `Retry` (`int`) - Obbligatorietà: obbligatoria. Numero tentativi di invio. GreaterThanOrEqualTo(0) (da validator).
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

Nessuna relazione persistente esplicita individuata.

### Enum usati

- `EmailStatus` — usato da `Email.Status`.

### Validator associati

`EmailValidator` (`src/RugbyRadio/Lib/Repositories/EmailBox/EmailValidator.cs`) — Regole: `Id` NotEmpty MaxLength 20; `UserId` NotEmpty MaxLength 20; `Subject` NotEmpty MaxLength 100; `Body` NotEmpty; `Status` IsInEnum; `Retry` GreaterThanOrEqualTo(0).

### Repository collegati

- `EmailRepository` — persiste e interroga `Email`.

### Service backend collegati

- `EmailService` — gestisce l'invio email tramite `EmailRepository` (relazione dedotta dal contesto; dettaglio non analizzato).

### Note di confidenza

Verificato dal codice.

---

## SystemMessage

### Nome nel codice

`SystemMessage`

### Nome wiki suggerito

`SystemMessage (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/SystemMessageBox/SystemMessage.cs`

### Namespace

`Lib.Repositories.SystemMessageBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<SystemMessage> SystemMessages` in `AppDbContext`
- `[Table("SystemMessages")]`
- `SystemMessageRepository` persiste e interroga questa entity

### Sintesi tecnica

Tabella di sistema per la localizzazione dei messaggi usati dalla piattaforma (commenti eventi, notifiche, errori). Ogni messaggio ha un codice, una lingua, una versione e un flag per messaggi con nome giocatore. Supporta varianti maschile/femminile.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `Code` (`string?`) - Obbligatorietà: opzionale. Codice identificativo del messaggio (es. `MatchEventType-300`).
- `Language` (`string?`) - Obbligatorietà: opzionale. Codice lingua (es. `IT`, `EN`).
- `FlagPlayer` (`bool?`) - Obbligatorietà: opzionale. `true` se il messaggio include il nome del giocatore.
- `Message` (`string?`) - Obbligatorietà: opzionale. Testo maschile.
- `MessageFemale` (`string?`) - Obbligatorietà: opzionale. Testo femminile.
- `Version` (`int?`) - Obbligatorietà: opzionale. Versione del messaggio.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

Nessuna relazione persistente esplicita individuata.

### Enum usati

Nessuno.

### Validator associati

`SystemMessageValidator` (`src/RugbyRadio/Lib/Repositories/SystemMessageBox/SystemMessageValidator.cs`) — Nessuna regola dichiarata (tabella di sistema, validazione disabilitata).

### Repository collegati

- `SystemMessageRepository` — persiste e interroga `SystemMessage`.

### Service backend collegati

- `RugbyRadioExceptionFilter` — interroga `SystemMessageRepository` per tradurre i codici errore in messaggi localizzati.

### Note di confidenza

Verificato dal codice.

---

## SystemMessageDraft

### Nome nel codice

`SystemMessageDraft`

### Nome wiki suggerito

`SystemMessageDraft (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/SystemMessageDraftBox/SystemMessageDraft.cs`

### Namespace

`Lib.Repositories.SystemMessageDraftBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<SystemMessageDraft> SystemMessageDrafts` in `AppDbContext`
- `[Table("SystemMessagesDraft")]`
- `SystemMessageDraftRepository` persiste e interroga questa entity

### Sintesi tecnica

Tabella di staging per i messaggi di sistema in attesa di approvazione. Contiene gli stessi campi di `SystemMessage` più campi specifici per il processo di revisione: `GroupId` (raggruppamento logico), `VerifyValue`, `VerifyLanguageValue` (percentuali di verifica) e `IsVerified` (flag di approvazione).

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria.

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `Code` (`string?`) - Obbligatorietà: opzionale. Codice del messaggio.
- `Language` (`string?`) - Obbligatorietà: opzionale. Codice lingua.
- `FlagPlayer` (`bool?`) - Obbligatorietà: opzionale. `true` se il messaggio include il nome del giocatore.
- `Message` (`string?`) - Obbligatorietà: opzionale. Testo del messaggio.
- `GroupId` (`string`) - Obbligatorietà: obbligatoria. Non nullable; default `string.Empty`. Raggruppamento logico dei messaggi.
- `VerifyValue` (`int`) - Obbligatorietà: obbligatoria. Percentuale di verifica contenuto.
- `VerifyLanguageValue` (`int`) - Obbligatorietà: obbligatoria. Percentuale di verifica lingua.
- `Version` (`int?`) - Obbligatorietà: opzionale. Versione del messaggio.
- `IsVerified` (`bool`) - Obbligatorietà: obbligatoria. `true` se il messaggio è stato approvato.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

Nessuna relazione persistente esplicita individuata.

### Enum usati

Nessuno.

### Validator associati

`SystemMessageDraftValidator` (`src/RugbyRadio/Lib/Repositories/SystemMessageDraftBox/SystemMessageDraftValidator.cs`) — Nessuna regola dichiarata (tabella di sistema, validazione disabilitata).

### Repository collegati

- `SystemMessageDraftRepository` — persiste e interroga `SystemMessageDraft`.

### Service backend collegati

- `AdminV1Controller` — legge, aggiorna e approva draft tramite `SystemMessageDraftRepository` (accesso diretto da controller).

### Note di confidenza

Verificato dal codice.

---

## Audit

### Nome nel codice

`Audit`

### Nome wiki suggerito

`Audit (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/AuditBox/Audit.cs`

### Namespace

`Lib.Repositories.AuditBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- Configurata in `AppDbContext.OnModelCreating` (conversioni custom per `RequestSerialized` e `ResponseSerialized`)
- Estende `EntityBase`
- `AuditRepository` persiste questa entity
- `AuditMiddleware` la popola e la salva ad ogni richiesta HTTP

### Sintesi tecnica

Rappresenta un log di audit di una richiesta HTTP. Contiene metodo, path, corpo richiesta e risposta (compressi), status code, tempo di risposta, user ID, lingua e nome operazione interna. I campi serializzati sono compressi con `DbConfiguratorHelper.Zip/Unzip`.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria; NotNull MaxLength 20 (da validator).

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `ResponseTime` (`long?`) - Obbligatorietà: opzionale. Tempo di risposta in millisecondi.
- `ResponseStatus` (`int?`) - Obbligatorietà: opzionale. HTTP status code della risposta.
- `UserLanguage` (`string?`) - Obbligatorietà: opzionale. Lingua utente da header `X-USER-LANGUAGE`.
- `CommentaryLanguage` (`string?`) - Obbligatorietà: opzionale. Lingua commentary da header `X-COMMENTARY-LANGUAGE`.
- `RequestPath` (`string?`) - Obbligatorietà: opzionale. Path e query string della richiesta.
- `RequestMethod` (`string?`) - Obbligatorietà: opzionale. Metodo HTTP (GET, POST, ecc.).
- `RequestInternalName` (`string?`) - Obbligatorietà: opzionale. Nome operazione interno (da `RouteNameMetadata`).
- `RequestSerialized` (`string?`) - Obbligatorietà: opzionale. Corpo richiesta serializzato e compresso.
- `ResponseSerialized` (`string?`) - Obbligatorietà: opzionale. Corpo risposta serializzato e compresso.
- `UserId` (`string?`) - Obbligatorietà: opzionale. ID utente corrente (null se anonimo).
- `Message` (`string?`) - Obbligatorietà: opzionale. Messaggio di errore se presente.
- `LongMessage` (`string?`) - Obbligatorietà: opzionale. Inner exception o stack trace se presente.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

Nessuna relazione persistente esplicita individuata.

### Enum usati

Nessuno.

### Validator associati

`AuditValidator` (`src/RugbyRadio/Lib/Repositories/AuditBox/AuditValidator.cs`) — Regole: `Id` NotNull MaxLength 20.

### Repository collegati

- `AuditRepository` — persiste `Audit`.

### Service backend collegati

- `AuditMiddleware` — popola e salva `Audit` per ogni richiesta HTTP (tramite `AuditRepository`).

### Note di confidenza

Verificato dal codice.

### Note

Il nome tabella DB non è deducibile dal codice letto (attributo `[Table]` assente sull'entity; possibile configurazione separata o nome convenzionale `Audits`).

---

## FakeAgentSetting

### Nome nel codice

`FakeAgentSetting`

### Nome wiki suggerito

`FakeAgentSetting (api)`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/FakeAgentBox/FakeAgentSetting.cs`

### Namespace

`Lib.Repositories.FakeAgentBox`

### Progetto o modulo

`Lib`

### Tipo

Entity persistente

### Evidenza di persistenza

- `DbSet<FakeAgentSetting> FakeAgentSettings` in `AppDbContext`
- `[Table("FakeAgentSettings")]`
- `FakeAgentSettingRepository` persiste e interroga questa entity

### Sintesi tecnica

Rappresenta la configurazione per un agente simulatore di partite (Fake Agent). Contiene parametri per la generazione automatica di partite simulate: anno/mese, canale base, squadra principale, lingua, limiti di generazione e data della prossima partita.

### Identificativi

- `Id` (`string`) — ereditato; chiave primaria; NotNull MaxLength 20 (da validator).

### Proprietà

- `Id` (`string`) - Obbligatorietà: obbligatoria. Chiave primaria. Ereditata.
- `Year` (`int`) - Obbligatorietà: obbligatoria. Anno di configurazione.
- `Month` (`int`) - Obbligatorietà: obbligatoria. Mese di configurazione.
- `ChannelId` (`string`) - Obbligatorietà: obbligatoria. Non nullable; default `string.Empty`. FK verso `Channel`.
- `MainTeamId` (`string`) - Obbligatorietà: obbligatoria. Non nullable. FK verso `Team` principale.
- `Language` (`string`) - Obbligatorietà: obbligatoria. Non nullable. Lingua da usare per la generazione.
- `MaxTeams` (`int`) - Obbligatorietà: obbligatoria. Numero massimo di squadre da creare.
- `MaxMatches` (`int`) - Obbligatorietà: obbligatoria. Numero massimo di partite da creare.
- `NextMatchDate` (`DateTime?`) - Obbligatorietà: opzionale. Data prossima partita da giocare.
- `ViewerIds` (`string?`) - Obbligatorietà: opzionale. ID utenti spettatori; formato serializzazione non deducibile dal codice entity.
- `IsDeleted` (`bool`) - Obbligatorietà: obbligatoria. Ereditata.
- `Ts` (`DateTime`) - Obbligatorietà: obbligatoria. Ereditata.

### Relazioni

Nessuna navigation property esplicita sull'entity (FK `ChannelId` e `MainTeamId` presenti come proprietà scalari).

### Enum usati

Nessuno.

### Validator associati

`FakeAgentValidator` (`src/RugbyRadio/Lib/Repositories/FakeAgentBox/FakeAgentValidator.cs`) — Regole: `Id` NotNull MaxLength 20.

### Repository collegati

- `FakeAgentSettingRepository` — persiste e interroga `FakeAgentSetting`.

### Service backend collegati

Non deducibili direttamente dall'analisi dei controller API.

### Note di confidenza

Verificato dal codice.

---

## Enum

---

## MatchStatus

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchStatus.cs`

### Valori

- `Scheduled = 10` — Da giocare.
- `InProgress = 20` — In corso.
- `Halftime = 25` — Fine primo tempo / pausa.
- `FullTime = 30` — Terminata.

### Usato da

- `Match.Status` — conversione `HasConversion<int>()` in `AppDbContext.OnModelCreating`.

---

## MatchEventType

### File sorgente

- `src/RugbyRadio/Lib/Types/MatchEventType.cs`

### Valori (raggruppati per categoria)

- `Fake = 0` — Evento fittizio di supporto.
- Calcio d'inizio: `Kickoff = 100`, `KickoffHalfTime = 110`.
- Meteo: `WeatherTypeSun = 101`, `WeatherTypeCloudSun = 102`, `WeatherTypeCloud = 103`, `WeatherTypeCloudRain = 104`, `WeatherTypeCloudSnow = 105`, `WeatherTypeFog = 106`.
- Temperatura: `TemperatureVeryLow = 151`, `TemperatureLow = 152`, `TemperatureHalf = 153`, `TemperatureHigh = 154`.
- `FullTime = 200` — Fine partita.
- `Try = 300` — Meta.
- `Conversion = 400` — Conversione.
- `ConversionFailed = 500` — Conversione fallita.
- `Penalty = 600` — Punizione.
- `PenaltyFailed = 700` — Punizione fallita.
- `TryPenalty = 800` — Meta tecnica.
- Segnali arbitro: `Signal = 900` … `SignalTry = 951` (51 valori, tutti con value `90x`–`951`).
- `Scrum = 1000` — Mischia.
- `Touche = 1100` — Touche.
- `Injury = 1200` — Infortunio.
- `Kick = 1300` — Calcio.
- `YellowCard = 1400` — Cartellino giallo.
- `RedCard = 1500` — Cartellino rosso.
- `HalfTime = 1600` — Fine primo tempo.
- `War = 1700` — War.
- `ForwardPass = 1800` — In avanti.
- `TenMt = 1900` — 10 metri.
- `ToucheFoul = 2000` — Touche storta.
- `Offside = 2100` — Fuorigioco.
- `Substitution = 2200` — Sostituzione.
- `Headset = 2300` — Commento libero.
- `ErrataCorrige = 2400` — Errata corrige punteggio.
- `Tackle = 2500` — Placcaggio.
- `HeldUp = 2600` — Tenuta alta.

### Usato da

- `MatchEvent.Type` — conversione `HasConversion<int>()` in `AppDbContext.OnModelCreating`.

---

## TerritoryType

### File sorgente

- `src/RugbyRadio/Lib/Types/TerritoryType.cs`

### Valori

- `Zone1T1 = 110` — Prima zona a sinistra, lato T1.
- `Zone2T1 = 120` — Seconda zona a sinistra, lato T1.
- `Zone3T1 = 130` — Seconda zona a destra, lato T1.
- `Zone4T1 = 140` — Prima zona a destra, lato T1.
- `Zone1T2 = 210` — Prima zona a sinistra, lato T2.
- `Zone2T2 = 220` — Seconda zona a sinistra, lato T2.
- `Zone3T2 = 230` — Seconda zona a destra, lato T2.
- `Zone4T2 = 240` — Prima zona a destra, lato T2.

### Usato da

- `MatchEvent.Territory` — conversione `HasConversion<int>()` in `AppDbContext.OnModelCreating`.

---

## EmailStatus

### File sorgente

- `src/RugbyRadio/Lib/Repositories/EmailBox/EmailStatus.cs`

### Valori

- `Pending = 10` — Email in sospeso, non ancora inviata.
- `Sent = 20` — Email inviata con successo.
- `Failed = 30` — Invio fallito.

### Usato da

- `Email.Status` — validato con `IsInEnum` dal `EmailValidator`.

---

## Candidati esclusi o incerti

---

## MatchScoreboard

### Motivo

- Non persistente: nessun `DbSet`, nessun `[Table]`, nessuna ereditarietà da `EntityBase`.
- Classe di modello in-memory usata probabilmente per costruire tabellini di partita.

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchScoreboard.cs`

---

## MatchScoreboardAi

### Motivo

- Non persistente: stesse condizioni di `MatchScoreboard`.
- Variante del tabellino ottimizzata per elaborazione AI.

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchScoreboardAi.cs`

---

## EventReactionCount

### Motivo

- Non persistente: classe di aggregazione definita nello stesso file di `EventReaction`.
- Nessun `DbSet`, nessun `[Table]`, nessuna ereditarietà da `EntityBase`.
- Usata per restituire conteggi aggregati di reazioni.

### File sorgente

- `src/RugbyRadio/Lib/Repositories/EventReactionBox/EventReaction.cs`

---

## SystemMessageVersion

### Motivo

- Non persistente: classe di supporto definita nel namespace `SystemMessageBox`.
- Nessun `DbSet`, nessun `[Table]`, nessuna ereditarietà da `EntityBase`.

### File sorgente

- `src/RugbyRadio/Lib/Repositories/SystemMessageBox/SystemMessageVersion.cs`

---

## CheckSentencesAnswer

### Motivo

- Non persistente: classe di risposta per verifica frasi definita nel namespace `SystemMessageBox`.
- Nessun `DbSet`, nessun `[Table]`, nessuna ereditarietà da `EntityBase`.

### File sorgente

- `src/RugbyRadio/Lib/Repositories/SystemMessageBox/CheckSentencesAnswer.cs`

---

## Note finali

- Limiti dell'analisi: il dettaglio dei repository non è stato seguito in profondità per ogni entity; i service collegati sono indicati solo dove deducibili direttamente dal codice analizzato nei RAW precedenti; `MatchScoreboard`, `MatchScoreboardAi` e classi correlate non sono stati letti per intero.
- Elementi non deducibili: nome tabella DB di `MatchLike` (assenza di `[Table]`); nome tabella DB di `Audit` (assenza di `[Table]`); meccanismo di hashing della password in `User.Password`; formato di `FakeAgentSetting.ViewerIds`; valori ammessi per `EventReaction.ReactionType`.
- Possibili approfondimenti: analisi completa di `MatchScoreboard` e `MatchScoreboardAi` per classificarli con certezza; verifica meccanismo hashing password; analisi helper `DbConfiguratorHelper` per dettaglio compressione audit.
- Wiki non modificata: confermato.
