# Backend Entities RAW

## Sintesi

- Data analisi: 2026-05-19
- Root backend analizzata: `src/RugbyRadio`
- Filtro applicato: soli file modificati dopo il 2026-05-14
- File output: `llm-wiki/raw/backend/api/entities-20260519.md`
- Entity backend trovate: 3
- Enum trovati: 1
- Validator collegati trovati: 3
- Repository collegati trovati: 3
- Service collegati trovati: 1
- Elementi incerti o esclusi: 3

## Criteri usati

- Sono state classificate come entity backend solo classi usate da repository modificati come modelli persistenti.
- Non sono stati classificati come entity i DTO, le classi statiche, i settings, i job, i writer e i service.
- I file sorgente diretti delle entity non risultano modificati dopo il 2026-05-14; quindi proprietà, namespace e relazioni sono documentati solo quando visibili nei repository o service modificati.
- I validator sono indicati solo quando iniettati direttamente nei repository modificati.
- I service backend sono indicati solo quando leggono entity in modo verificabile tramite repository modificati.

## Entity backend

## Channel

### Nome nel codice

`Channel`

### Nome wiki suggerito

`ChannelEntity`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/ChannelBox/Channel.cs`

### Namespace

`Non deducibile dai soli file modificati analizzati`

### Progetto o modulo

`Lib.Repositories.ChannelBox`

### Tipo

Entity persistente

### Evidenza di persistenza

- Repository: `ChannelRepository : Repository<Channel>, IChannelRepository`
- Interfaccia repository: `IChannelRepository : IRepository<Channel>`
- Query EF tramite `GetQuery()`
- Include su navigation property `Owners`, `User`, `Matches`

### Sintesi tecnica

Entity persistente dei canali, letta e filtrata da `ChannelRepository` per lookup pubblico, ownership utente, ricerca e candidati SEO.

### Identificativi

- `Id` (`string`) - Chiave primaria deducibile da `GetByIdAsync(string id)`.
- `PublicId` (`string?`) - Identificativo pubblico deducibile da `GetByPublicIdIncludeAsync`, `GetByPublicIdAsync` e `FindSeoCandidatesAsync`.
- `UserId` (`string?`) - Identificativo utente proprietario deducibile dai filtri ownership.

### Proprietà

- `Id` (`string`) - Obbligatorietà: non deducibile. Identificativo usato nel lookup per ID.
- `PublicId` (`string?`) - Obbligatorietà: opzionale. Identificativo pubblico usato nei lookup pubblici e nei candidati SEO.
- `UserId` (`string?`) - Obbligatorietà: opzionale. Identificativo utente usato nei filtri ownership.
- `Name` (`string?`) - Obbligatorietà: non deducibile. Campo ricercabile in `FindByFilterAsync`.
- `HeaderTitle` (`string?`) - Obbligatorietà: non deducibile. Campo ricercabile in `FindByFilterAsync`.
- `HeaderSubtitle` (`string?`) - Obbligatorietà: non deducibile. Campo ricercabile in `FindByFilterAsync`.
- `IsDeleted` (`bool`) - Obbligatorietà: non deducibile. Flag usato per escludere record cancellati.
- `IsTestChannel` (`bool?`) - Obbligatorietà: opzionale. Flag usato per escludere canali di test dai risultati pubblici/SEO.
- `Ts` (`DateTime`) - Obbligatorietà: non deducibile. Timestamp usato dal service SEO per calcolare `lastmod`.
- `Owners` (`ICollection` o equivalente) - Obbligatorietà: non deducibile. Navigation usata per ownership/co-ownership.
- `User` (`User`) - Obbligatorietà: non deducibile. Navigation usata in include e ricerca su nickname.
- `Matches` (`ICollection` o equivalente) - Obbligatorietà: non deducibile. Navigation usata per ordinamento e `lastmod`.

### Relazioni

- `User` - Tipo relazione: navigation. Evidenza: `Include(x => x.User)` e `x.User.Nickname`.
- `ChannelUser` - Tipo relazione: navigation. Evidenza: `Include(x => x.Owners)` e `x.Owners!.Any(y => y.UserId == userId.ToUpper())`.
- `Match` - Tipo relazione: navigation. Evidenza: `Include(x => x.Matches)` e `channel.Matches`.

### Enum usati

Nessun enum deducibile dai file modificati analizzati.

### Validator associati

- `IValidator<Channel>` - Iniettato nel costruttore di `ChannelRepository`; regole principali non deducibili dai soli file modificati.

### Repository collegati

- `ChannelRepository` - usa o persiste `Channel`.

### Service backend collegati

- `SeoUrlInventoryService` - legge `Channel` tramite `IChannelRepository.FindSeoCandidatesAsync()`.

### Note di confidenza

Parzialmente dedotto

### Note

Il file sorgente diretto `Channel.cs` non risulta modificato nel periodo, quindi proprietà complete e namespace effettivo non sono stati arricchiti dal file entity.

## Match

### Nome nel codice

`Match`

### Nome wiki suggerito

`MatchEntity`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/Match.cs`

### Namespace

`Non deducibile dai soli file modificati analizzati`

### Progetto o modulo

`Lib.Repositories.MatchBox`

### Tipo

Entity persistente

### Evidenza di persistenza

- Repository: `MatchRepository : Repository<Match>, IMatchRepository`
- Interfaccia repository: `IMatchRepository : IRepository<Match>`
- Query EF tramite `GetQuery()`
- Include su navigation property `HomeTeam`, `AwayTeam`, `Channel`, `Events`, `Blogs`

### Sintesi tecnica

Entity persistente delle partite, letta e filtrata da `MatchRepository` per canale, stato, ownership utente, blog mancanti, immagini mancanti, data e candidati SEO.

### Identificativi

- `Id` (`string`) - Chiave primaria deducibile da `GetByIdIncludeAsync(string matchId)` e URL SEO `/g-match/{match.Id}`.
- `ChannelId` (`string?`) - Foreign key deducibile dai filtri repository.
- `HomeTeamId` (`string?`) - Foreign key deducibile dalla policy SEO.
- `AwayTeamId` (`string?`) - Foreign key deducibile dalla policy SEO.

### Proprietà

- `Id` (`string`) - Obbligatorietà: non deducibile. Identificativo usato in lookup e URL SEO.
- `ChannelId` (`string?`) - Obbligatorietà: opzionale. Foreign key usata nei filtri per canale.
- `HomeTeamId` (`string?`) - Obbligatorietà: opzionale. Foreign key usata nella policy SEO.
- `AwayTeamId` (`string?`) - Obbligatorietà: opzionale. Foreign key usata nella policy SEO.
- `Status` (`MatchStatus`) - Obbligatorietà: non deducibile. Stato usato nei filtri repository e nella policy SEO.
- `DateUtc` (`DateTime`) - Obbligatorietà: non deducibile. Data usata per ordinamento, filtri e `lastmod`.
- `Ts` (`DateTime`) - Obbligatorietà: non deducibile. Timestamp usato per `lastmod`.
- `IsDeleted` (`bool`) - Obbligatorietà: non deducibile. Flag usato per escludere record cancellati.
- `HomeTeam` (`Team`) - Obbligatorietà: non deducibile. Navigation inclusa in query repository.
- `AwayTeam` (`Team`) - Obbligatorietà: non deducibile. Navigation inclusa in query repository.
- `Channel` (`Channel`) - Obbligatorietà: non deducibile. Navigation inclusa in query repository e policy SEO.
- `Events` (`ICollection` o equivalente) - Obbligatorietà: non deducibile. Navigation usata per include e conteggio eventi.
- `Blogs` (`ICollection` o equivalente) - Obbligatorietà: non deducibile. Navigation usata per verificare blog statico e conteggio blog.

### Relazioni

- `Channel` - Tipo relazione: foreign key. Evidenza: `ChannelId` e `Include(x => x.Channel)`.
- `Team` - Tipo relazione: foreign key. Evidenza: `HomeTeamId`, `AwayTeamId`, `HomeTeam`, `AwayTeam`.
- `MatchEvent` - Tipo relazione: navigation. Evidenza: `Include(x => x.Events)` e `match.Events?.Count`.
- `Blog` - Tipo relazione: navigation. Evidenza: `Include(x => x.Blogs)` e `match.Blogs?.Any(blog => blog.IsBlogStatic == true)`.
- `LineupPlayer` - Tipo relazione: navigation. Evidenza: `Events.LineupPlayer`.
- `Player` - Tipo relazione: navigation. Evidenza: `Events.LineupPlayer.Player`.

### Enum usati

- `MatchStatus` - Usato da `Status`.

### Validator associati

- `IValidator<Match>` - Iniettato nel costruttore di `MatchRepository`; regole principali non deducibili dai soli file modificati.

### Repository collegati

- `MatchRepository` - usa o persiste `Match`.

### Service backend collegati

- `SeoUrlInventoryService` - legge `Match` tramite `IMatchRepository.FindSeoCandidatesAsync()`.

### Note di confidenza

Parzialmente dedotto

### Note

Il file sorgente diretto `Match.cs` non risulta modificato nel periodo, quindi proprietà complete, namespace effettivo e configurazioni ORM non sono stati arricchiti dal file entity.

## Team

### Nome nel codice

`Team`

### Nome wiki suggerito

`TeamEntity`

### File sorgente

- `src/RugbyRadio/Lib/Repositories/TeamBox/Team.cs`

### Namespace

`Non deducibile dai soli file modificati analizzati`

### Progetto o modulo

`Lib.Repositories.TeamBox`

### Tipo

Entity persistente

### Evidenza di persistenza

- Repository: `TeamRepository : Repository<Team>, ITeamRepository`
- Interfaccia repository: `ITeamRepository : IRepository<Team>`
- Query EF tramite `GetQuery()`
- Include su navigation property `Channel`

### Sintesi tecnica

Entity persistente delle squadre, letta e filtrata da `TeamRepository` per canale, ID e candidati SEO.

### Identificativi

- `Id` (`string?`) - Chiave primaria deducibile da `GetByIdIncludeAsync(string teamId)` e URL SEO `/g-team/{team.Id}`.
- `ChannelId` (`string?`) - Foreign key deducibile da `FindByChannelAsync`.

### Proprietà

- `Id` (`string?`) - Obbligatorietà: opzionale. Identificativo usato in lookup, filtro non null e URL SEO.
- `ChannelId` (`string?`) - Obbligatorietà: opzionale. Foreign key usata per recuperare squadre associate a un canale.
- `Name` (`string?`) - Obbligatorietà: opzionale. Campo richiesto per candidati SEO.
- `IsDeleted` (`bool`) - Obbligatorietà: non deducibile. Flag usato per escludere record cancellati.
- `Ts` (`DateTime`) - Obbligatorietà: non deducibile. Timestamp usato per `lastmod`.
- `Channel` (`Channel`) - Obbligatorietà: non deducibile. Navigation inclusa in query repository.

### Relazioni

- `Channel` - Tipo relazione: foreign key. Evidenza: `ChannelId`, `Include(x => x.Channel)` e `team.Channel`.
- `User` - Tipo relazione: navigation. Evidenza: `ThenInclude(x => x!.User)` tramite `Channel`.

### Enum usati

Nessun enum deducibile dai file modificati analizzati.

### Validator associati

- `IValidator<Team>` - Iniettato nel costruttore di `TeamRepository`; regole principali non deducibili dai soli file modificati.

### Repository collegati

- `TeamRepository` - usa o persiste `Team`.

### Service backend collegati

- `SeoUrlInventoryService` - legge `Team` tramite `ITeamRepository.FindSeoCandidatesAsync()`.

### Note di confidenza

Parzialmente dedotto

### Note

Il file sorgente diretto `Team.cs` non risulta modificato nel periodo, quindi proprietà complete, namespace effettivo e configurazioni ORM non sono stati arricchiti dal file entity.

## Enum

## MatchStatus

### File sorgente

- `src/RugbyRadio/Lib/Repositories/MatchBox/MatchStatus.cs`

### Valori

- `InProgress` - valore usato nei filtri repository.
- `Halftime` - valore usato nei filtri repository.
- `Scheduled` - valore usato nei filtri repository.
- `FullTime` - valore usato nei filtri repository e nella policy SEO.
- Altri valori: non deducibili dai soli file modificati analizzati.

### Usato da

- `Match.Status`

## Candidati esclusi o incerti

## SeoPublicUrl

### Motivo

- DTO backend
- Non persistente
- Altro: il commento sorgente dichiara che non e una database entity ed e ricostruita da route frontend, repository e riferimenti sitemap blog.

### File sorgente

- `src/RugbyRadio/HF/Dto/SeoPublicUrl.cs`

## SeoPublicUrlRules

### Motivo

- Helper
- Classe statica
- Non persistente

### File sorgente

- `src/RugbyRadio/HF/Dto/SeoPublicUrlRules.cs`

## SeoSitemapSettings

### Motivo

- Altro: classe settings/configurazione
- Non persistente

### File sorgente

- `src/RugbyRadio/Lib/Settings/SeoSitemapSettings.cs`

## Note finali

- Limiti dell'analisi:
  - Il filtro richiesto limita l'analisi ai soli file modificati dopo il 2026-05-14.
  - I file sorgente diretti delle entity backend non risultano modificati nel periodo e non sono stati usati per completare il modello.
  - `AppDbContext` non risulta modificato nel periodo, quindi `DbSet`, mapping EF e configurazioni `OnModelCreating` non sono stati censiti.
- Elementi non deducibili:
  - Namespace effettivo delle entity.
  - Obbligatorietà completa delle proprietà.
  - Cardinalità completa delle relazioni.
  - Valori completi di `MatchStatus`.
  - Regole dei validator collegati.
  - Configurazioni ORM, indici, vincoli e seed.
- Possibili approfondimenti:
  - Eseguire helper senza filtro temporale per censire entity sorgente, DbContext, enum completi e validator completi.
- Conferma:
  - Nessun file in `llm-wiki/wiki/` e stato creato o modificato.
