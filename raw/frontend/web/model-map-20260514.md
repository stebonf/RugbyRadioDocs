# Frontend Model Map RAW

## Sintesi

- Data analisi: 2026-05-14
- Project: web
- Root frontend analizzata: `src/RugbyRadioWeb/src/app/dto/`
- File output: `llm-wiki/raw/frontend/web/model-map-20260514.md`
- Modelli FE trovati: 8 (view model/type definiti in componenti/servizi)
- DTO FE trovati: 52 (in cartella `dto/`)
- Enum FE trovati: 4
- Consumer FE deducibili: da analisi component-map e service-map
- API correlate deducibili: 59 (via service-map)
- Candidati esclusi o incerti: 0

---

## DTO frontend

---

## responseDto

### Nome nel codice

`responseDto`

### Nome wiki suggerito

`responseDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/commonDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Risposta generica di errore/stato dal backend. Contiene codice, messaggio e mappa errori per campo.

### Proprietà

- `code` (`string`) - Obbligatorietà: opzionale. Codice risposta (es. `"1000"`, `"OK"`, `"EX"`).
- `message` (`string`) - Obbligatorietà: opzionale. Messaggio descrittivo.
- `errors` (`Record<string, string>`) - Obbligatorietà: opzionale. Mappa campo → messaggio di errore.

### Origine dati

API backend — risposta di errore 400

### Consumer FE

- `ErrorHandlerService`, pagine con gestione errori

### API correlate

- `RugbyRadioExceptionFilter` (backend) — produce questo DTO su `BadRequest 400`

### Note di confidenza

Verificato dal codice

---

## lookupDto

### Nome nel codice

`lookupDto`

### Nome wiki suggerito

`lookupDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/commonDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Coppia id/descrizione usata per campi lookup (es. stato partita).

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `description` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `matchDto.status`, `matchMinDto.status`, `matchChannelDto.status`

### Note di confidenza

Verificato dal codice

---

## pageDto\<T\>

### Nome nel codice

`pageDto<T>`

### Nome wiki suggerito

`pageDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/commonDto.ts`

### Tipo

DTO response — paginazione generica

### Sintesi tecnica

Wrapper paginato generico su `T`. Usato da tutti gli endpoint di lista paginata.

### Proprietà

- `currentPage` (`number`) - Obbligatorietà: obbligatoria.
- `pageSize` (`number`) - Obbligatorietà: obbligatoria.
- `totalPages` (`number`) - Obbligatorietà: obbligatoria.
- `totalItems` (`number`) - Obbligatorietà: obbligatoria.
- `items` (`T[]`) - Obbligatorietà: obbligatoria. Array degli elementi della pagina.

### Consumer FE

- `ChannelCardListComponent`, `MatchCardListComponent`, `EntityPaginationComponent`
- `ChannelService.findChannels`, `MatchService.findMatches`, `MatchService.findMatchesByChannel`, `MatchService.findMatchesByTeam`, `BlogService.getBlogs`

### Note di confidenza

Verificato dal codice

---

## userLoginDto

### Nome nel codice

`userLoginDto`

### Nome wiki suggerito

`userLoginDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO request

### Sintesi tecnica

Payload di login email+password.

### Proprietà

- `email` (`string`) - Obbligatorietà: obbligatoria.
- `password` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `UserService.login` → `LoginPage`

### API correlate

- `AuthV1Controller.Login` — POST `/auth/user/login` (ATH-03)

### Note di confidenza

Verificato dal codice

---

## userTokenDto

### Nome nel codice

`userTokenDto`

### Nome wiki suggerito

`userTokenDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Token JWT e dati utente restituiti dopo login/registrazione. Persistito in `localStorage` tramite `UserService.setSession`.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria. ID utente.
- `nickname` (`string`) - Obbligatorietà: obbligatoria. Nome pubblico.
- `token` (`string`) - Obbligatorietà: obbligatoria. JWT Bearer token.
- `language` (`string`) - Obbligatorietà: obbligatoria. Lingua preferita.

### Consumer FE

- `UserService.login`, `UserService.register`, `UserService.loginWithGoogle`, `UserService.changePassword`
- `LoginPage`, `RegistrationPage`, `ResetPasswordPage`

### API correlate

- `AuthV1Controller` — ATH-01, ATH-03, ATH-04, ATH-06

### Note di confidenza

Verificato dal codice

---

## userMinDto

### Nome nel codice

`userMinDto`

### Nome wiki suggerito

`userMinDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Rappresentazione minimale utente usata in relazioni (owner canale, autore commento).

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `nickname` (`string`) - Obbligatorietà: obbligatoria.
- `avatarUrl` (`string`) - Obbligatorietà: obbligatoria.
- `language` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `channelMinDto.owner`, `matchCommentDto.user`, `GTeamPage`

### Note di confidenza

Verificato dal codice

---

## userRegisterDto

### Nome nel codice

`userRegisterDto`

### Nome wiki suggerito

`userRegisterDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO request

### Proprietà

- `email` (`string`) - Obbligatorietà: obbligatoria.
- `password` (`string`) - Obbligatorietà: obbligatoria.
- `nickname` (`string`) - Obbligatorietà: obbligatoria.
- `language` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `UserService.register` → `RegistrationPage`

### API correlate

- `AuthV1Controller.Register` — ATH-01

### Note di confidenza

Verificato dal codice

---

## userGoogleRegisterDto

### Nome nel codice

`userGoogleRegisterDto`

### Nome wiki suggerito

`userGoogleRegisterDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO request

### Proprietà

- `language` (`string`) - Obbligatorietà: obbligatoria.
- `token` (`string`) - Obbligatorietà: obbligatoria. Google ID Token.

### Consumer FE

- `UserService.loginWithGoogle` → `LoginPage`

### API correlate

- `AuthV1Controller.GoogleLogin` — ATH-04

### Note di confidenza

Verificato dal codice

---

## userProfileDto

### Nome nel codice

`userProfileDto`

### Nome wiki suggerito

`userProfileDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Profilo completo utente autenticato con statistiche canali e follow.

### Proprietà

- `email` (`string`) - Obbligatorietà: obbligatoria.
- `nickname` (`string`) - Obbligatorietà: obbligatoria.
- `avatarUrl` (`string`) - Obbligatorietà: obbligatoria.
- `language` (`string`) - Obbligatorietà: obbligatoria.
- `timezone` (`string`) - Obbligatorietà: obbligatoria.
- `isVerified` (`boolean`) - Obbligatorietà: obbligatoria.
- `channels` (`number`) - Obbligatorietà: obbligatoria. Numero canali dell'utente.
- `followChannels` (`number`) - Obbligatorietà: obbligatoria. Numero canali seguiti.

### Consumer FE

- `UserService.getProfile`, `ProfilePage`, `HomeUserComponent`, `BaseUserComponent`

### API correlate

- `UserV1Controller.GetProfile` — USR-02

### Note di confidenza

Verificato dal codice

---

## userAvatarsDto

### Nome nel codice

`userAvatarsDto`

### Nome wiki suggerito

`userAvatarsDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO response — type alias

### Sintesi tecnica

Dizionario id (numerico) → path immagine avatar disponibili (1..58).

### Proprietà

- `Record<string, string>` — mappa numero → path avatar

### Consumer FE

- `UserService.getAvatars`, `ProfilePage`

### API correlate

- `UserV1Controller.GetAvatars` — USR-01

### Note di confidenza

Verificato dal codice

---

## userUpdateDto

### Nome nel codice

`userUpdateDto`

### Nome wiki suggerito

`userUpdateDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO request

### Proprietà

- `nickname` (`string`) - Obbligatorietà: opzionale.
- `avatarUrl` (`string`) - Obbligatorietà: opzionale.
- `language` (`string`) - Obbligatorietà: opzionale.
- `commentaryLanguage` (`string`) - Obbligatorietà: opzionale.

### Consumer FE

- `UserService.update`, `ProfilePage`; usato anche internamente da `setUserLanguage` e `setCommentatorId`

### API correlate

- `UserV1Controller.UpdateUser` — USR-03

### Note di confidenza

Verificato dal codice

---

## userTimezoneUpdateDto

### Nome nel codice

`userTimezoneUpdateDto`

### Nome wiki suggerito

`userTimezoneUpdateDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO request

### Proprietà

- `timezone` (`string`) - Obbligatorietà: opzionale.

### Consumer FE

- `UserService.updateTimezone`

### API correlate

- `UserV1Controller.UpdateTimezone` — USR-04

### Note di confidenza

Verificato dal codice

---

## userForgotPasswordDto / userChangePasswordDto / userFeedbackDto

### Nome nel codice

`userForgotPasswordDto`, `userChangePasswordDto`, `userFeedbackDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/userDto.ts`

### Tipo

DTO request

### Proprietà di userForgotPasswordDto

- `email` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di userChangePasswordDto

- `otpCode` (`string`) - Obbligatorietà: obbligatoria.
- `password` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di userFeedbackDto

- `rating` (`number`) - Obbligatorietà: obbligatoria.
- `message` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `UserService.forgotPassword` → `LoginPage`
- `UserService.changePassword` → `ResetPasswordPage`
- `UserService.sendFeedback` → `GFeedbackPage`

### API correlate

- ATH-05, ATH-06, USR-07

### Note di confidenza

Verificato dal codice

---

## channelPublicDto

### Nome nel codice

`channelPublicDto`

### Nome wiki suggerito

`channelPublicDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Canale pubblico completo con statistiche, squadre, classifica e stato follow utente corrente.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria. `publicId` del canale.
- `ownerId` (`string`) - Obbligatorietà: obbligatoria.
- `owner` (`string`) - Obbligatorietà: obbligatoria. Nickname owner.
- `ownerAvatar` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: obbligatoria.
- `headerBgColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerFtColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerTitle` (`string`) - Obbligatorietà: obbligatoria.
- `headerSubtitle` (`string`) - Obbligatorietà: obbligatoria.
- `lastMatchDate` (`string`) - Obbligatorietà: opzionale.
- `nextMatchDate` (`string`) - Obbligatorietà: opzionale.
- `matchesCount` (`number`) - Obbligatorietà: obbligatoria.
- `likeCount` (`number`) - Obbligatorietà: obbligatoria.
- `userFollow` (`boolean`) - Obbligatorietà: obbligatoria.
- `teams` (`teamChannelDto[]`) - Obbligatorietà: opzionale.
- `tables` (`channelTableDto[]`) - Obbligatorietà: opzionale.

### Enum usati

Nessuno

### Consumer FE

- `GChannelPage`, `ChannelCardComponent`, `ChannelCardListComponent`
- `ChannelService.getPublicChannel`, `ChannelService.findChannels`

### API correlate

- `ChannelsV1Controller.GetByPublicId` — CHL-10
- `ChannelsV1Controller.FindChannels` — CHL-11

### Note di confidenza

Verificato dal codice

---

## channelDto

### Nome nel codice

`channelDto`

### Nome wiki suggerito

`channelDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Canale privato (area utente). Contiene ID interno e publicId, dati layout, conteggio partite.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: obbligatoria.
- `publicId` (`string`) - Obbligatorietà: obbligatoria.
- `headerBgColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerFtColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerTitle` (`string`) - Obbligatorietà: obbligatoria.
- `headerSubtitle` (`string`) - Obbligatorietà: obbligatoria.
- `matches` (`number`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `ChannelsPage`, `FavoritesPage`, `HomeUserComponent`
- `ChannelService.getChannels`, `ChannelService.getChannel`

### API correlate

- CHL-01, CHL-02, CHL-03, CHL-04

### Note di confidenza

Verificato dal codice

---

## channelAddDto / channelUpdateDto / channelLayoutUpdateDto

### Nome nel codice

`channelAddDto`, `channelUpdateDto`, `channelLayoutUpdateDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelDto.ts`

### Tipo

DTO request

### Proprietà di channelAddDto

- `name` (`string`) - Obbligatorietà: obbligatoria.
- `headerBgColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerFtColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerTitle` (`string`) - Obbligatorietà: obbligatoria.
- `headerSubtitle` (`string`) - Obbligatorietà: opzionale.

### Proprietà di channelUpdateDto

- `name` (`string`) - Obbligatorietà: obbligatoria.
- `headerTitle` (`string`) - Obbligatorietà: obbligatoria.
- `headerSubtitle` (`string`) - Obbligatorietà: opzionale.

### Proprietà di channelLayoutUpdateDto

- `headerBgColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerFtColor` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `ChannelService.addChannel` → `ChannelsPage`
- `ChannelService.updateChannel`, `ChannelService.updateLayout` → `ChannelPage`

### API correlate

- CHL-03, CHL-04, CHL-05

### Note di confidenza

Verificato dal codice

---

## channelMinDto

### Nome nel codice

`channelMinDto`

### Nome wiki suggerito

`channelMinDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Rappresentazione minimale canale usata nelle relazioni di partite e squadre pubbliche.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: obbligatoria.
- `publicId` (`string`) - Obbligatorietà: obbligatoria.
- `owner` (`userMinDto`) - Obbligatorietà: obbligatoria.
- `headerBgColor` (`string`) - Obbligatorietà: opzionale.
- `headerFtColor` (`string`) - Obbligatorietà: opzionale.

### Consumer FE

- `matchMinDto.channel`, `teamPublicDto.channel`, `MatchCardSmallComponent`

### Note di confidenza

Verificato dal codice

---

## channelPublicMinDto

### Nome nel codice

`channelPublicMinDto`

### Nome wiki suggerito

`channelPublicMinDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Versione ancora più minimale del canale usata in `matchDto.channel` per i colori dell'header.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria. `publicId`.
- `idFull` (`string`) - Obbligatorietà: obbligatoria. ID interno.
- `headerBgColor` (`string`) - Obbligatorietà: obbligatoria.
- `headerFtColor` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `matchDto.channel`, `MatchHeaderComponent`

### Note di confidenza

Verificato dal codice

---

## channelTableDto

### Nome nel codice

`channelTableDto`

### Nome wiki suggerito

`channelTableDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Riga della classifica squadre di un canale.

### Proprietà

- `teamId` (`string`) - Obbligatorietà: obbligatoria.
- `teamName` (`string`) - Obbligatorietà: obbligatoria.
- `teamLogo` (`string`) - Obbligatorietà: obbligatoria.
- `played` (`number`) - Obbligatorietà: obbligatoria.
- `won` (`number`) - Obbligatorietà: obbligatoria.
- `lost` (`number`) - Obbligatorietà: obbligatoria.
- `drawn` (`number`) - Obbligatorietà: obbligatoria.
- `pointsFor` (`number`) - Obbligatorietà: obbligatoria.
- `pointsAgainst` (`number`) - Obbligatorietà: obbligatoria.
- `pointsDifference` (`number`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `channelPublicDto.tables`, `ChannelTablesComponent`, `GChannelPage`

### Note di confidenza

Verificato dal codice

---

## channelUserDto / channelUserAddDto

### Nome nel codice

`channelUserDto`, `channelUserAddDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/channelUserDto.ts`

### Tipo

DTO response / DTO request

### Proprietà di channelUserDto

- `id` (`string`) - Obbligatorietà: obbligatoria. ID utente.
- `name` (`string`) - Obbligatorietà: obbligatoria. Nickname.
- `email` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di channelUserAddDto

- `email` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `ChannelUserService`, `ChannelPage` (sub-componente `ChannelEditorsComponent`)

### API correlate

- CHL-USR-01, CHL-USR-02, CHL-USR-03

### Note di confidenza

Verificato dal codice

---

## matchDto

### Nome nel codice

`matchDto`

### Nome wiki suggerito

`matchDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Partita completa con formazioni, eventi, commenti, statistiche, scoreboard e stato utente (like, follow, is editor). DTO principale della pagina partita.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `date` (`string`) - Obbligatorietà: obbligatoria.
- `status` (`lookupDto`) - Obbligatorietà: opzionale.
- `homeTeamScore` (`number`) - Obbligatorietà: obbligatoria.
- `awayTeamScore` (`number`) - Obbligatorietà: obbligatoria.
- `minute` (`number`) - Obbligatorietà: obbligatoria.
- `homeTeam` (`teamDto`) - Obbligatorietà: opzionale.
- `awayTeam` (`teamDto`) - Obbligatorietà: opzionale.
- `stadium` (`unknown`) - Obbligatorietà: opzionale. Tipo non definito nel DTO.
- `homeTeamLineup` (`lineupPlayerDto[]`) - Obbligatorietà: opzionale.
- `awayTeamLineup` (`lineupPlayerDto[]`) - Obbligatorietà: opzionale.
- `events` (`matchEventDto[]`) - Obbligatorietà: opzionale.
- `comments` (`matchCommentDto[]`) - Obbligatorietà: opzionale.
- `homeStats` (`matchTeamStatsDto`) - Obbligatorietà: opzionale.
- `awayStats` (`matchTeamStatsDto`) - Obbligatorietà: opzionale.
- `stats` (`matchStatsDto`) - Obbligatorietà: opzionale.
- `channel` (`channelPublicMinDto`) - Obbligatorietà: opzionale.
- `scoreboard` (`matchScoreboardDto`) - Obbligatorietà: opzionale.
- `likes` (`number`) - Obbligatorietà: obbligatoria.
- `totalComments` (`number`) - Obbligatorietà: obbligatoria.
- `totalReactions` (`number`) - Obbligatorietà: obbligatoria.
- `userLineupRates` (`string[]`) - Obbligatorietà: opzionale. ID formazioni votate dall'utente corrente.
- `userLike` (`boolean`) - Obbligatorietà: obbligatoria.
- `userFollow` (`boolean`) - Obbligatorietà: obbligatoria.
- `isArchived` (`boolean`) - Obbligatorietà: obbligatoria.
- `isEditor` (`boolean`) - Obbligatorietà: obbligatoria. Indica se l'utente corrente è editor.

### Enum usati

- `matchStatus` — via `status.id`

### Consumer FE

- `GMatchPage`, `MatchPage`
- `MatchHeaderComponent`, `MatchEventsComponent`, `MatchLineupComponent`, `MatchStatsComponent`, `GMatchEventsComponent`
- `MatchService.getMatch`

### API correlate

- `MatchesV1Controller.GetMatchById` — MTC-02

### Note di confidenza

Verificato dal codice

---

## matchMinDto

### Nome nel codice

`matchMinDto`

### Nome wiki suggerito

`matchMinDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Partita in formato ridotto per liste e caroselli.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `date` (`string`) - Obbligatorietà: obbligatoria.
- `dateShort` (`string`) - Obbligatorietà: obbligatoria. Formato data abbreviato.
- `status` (`lookupDto`) - Obbligatorietà: opzionale.
- `homeTeamScore` (`number`) - Obbligatorietà: obbligatoria.
- `awayTeamScore` (`number`) - Obbligatorietà: obbligatoria.
- `homeTeam` (`teamDto`) - Obbligatorietà: opzionale.
- `awayTeam` (`teamDto`) - Obbligatorietà: opzionale.
- `channel` (`channelMinDto`) - Obbligatorietà: opzionale.
- `imageUrl` (`string`) - Obbligatorietà: opzionale.

### Consumer FE

- `MatchCardSmallComponent`, `MatchCardListComponent`
- `HomeLastMatchesComponent`, `HomeNextMatchesComponent`, `HomeOngoingMatchesComponent`
- `GMatchesPage`, `GChannelPage`, `GTeamPage`

### API correlate

- MTC-10, MTC-11, MTC-14, MTC-17, MTC-18

### Note di confidenza

Verificato dal codice

---

## matchChannelDto

### Nome nel codice

`matchChannelDto`

### Nome wiki suggerito

`matchChannelDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Partita dell'editor per lista canale, con flag di editabilità e data/ora.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `date` (`string`) - Obbligatorietà: obbligatoria.
- `dateShort` (`string`) - Obbligatorietà: obbligatoria.
- `time` (`string`) - Obbligatorietà: obbligatoria. Ora formattata.
- `status` (`lookupDto`) - Obbligatorietà: opzionale.
- `homeTeamScore` (`number`) - Obbligatorietà: obbligatoria.
- `awayTeamScore` (`number`) - Obbligatorietà: obbligatoria.
- `homeTeam` (`teamDto`) - Obbligatorietà: opzionale.
- `awayTeam` (`teamDto`) - Obbligatorietà: opzionale.
- `isEditEnabled` (`boolean`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `MatchService.getMatches`, `MatchService.getTrainingMatch`
- `HomeUserComponent`, `ChannelMatchesComponent`

### API correlate

- MTC-01, MTC-19

### Note di confidenza

Verificato dal codice

---

## matchAddDto / matchQuickAddDto / matchQuickItemAddDto / matchStatusUpdateDto

### Nome nel codice

`matchAddDto`, `matchQuickAddDto`, `matchQuickItemAddDto`, `matchStatusUpdateDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchDto.ts`

### Tipo

DTO request

### Proprietà di matchAddDto

- `dateDay` (`string`) - Obbligatorietà: opzionale.
- `dateHour` (`string | null`) - Obbligatorietà: opzionale.
- `homeTeamId` (`string | null`) - Obbligatorietà: opzionale.
- `awayTeamId` (`string | null`) - Obbligatorietà: opzionale.
- `timezone` (`string | null`) - Obbligatorietà: opzionale.

### Proprietà di matchQuickAddDto

- `dateDay` (`string`) - Obbligatorietà: obbligatoria.
- `channel` (`matchQuickItemAddDto`) - Obbligatorietà: obbligatoria.
- `homeTeam` (`matchQuickItemAddDto`) - Obbligatorietà: obbligatoria.
- `awayTeam` (`matchQuickItemAddDto`) - Obbligatorietà: obbligatoria.

### Proprietà di matchQuickItemAddDto

- `id` (`string`) - Obbligatorietà: opzionale. ID entità esistente.
- `name` (`string`) - Obbligatorietà: opzionale. Nome per entità nuova.

### Proprietà di matchStatusUpdateDto

- `status` (`number`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `MatchService.addMatch`, `MatchService.updateMatch`, `MatchService.addQuickMatch`, `MatchService.updateStatus`
- `MatchCreateComponent`, `MatchPage`

### API correlate

- MTC-03, MTC-08, MTC-09, MTC-17

### Note di confidenza

Verificato dal codice

---

## matchEventDto

### Nome nel codice

`matchEventDto`

### Nome wiki suggerito

`matchEventDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchEventDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Singolo evento di telecronaca di una partita. Contiene tipo, minuto, giocatori coinvolti, reazioni, commenti, immagine icona e testo generato.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `minute` (`number`) - Obbligatorietà: obbligatoria.
- `type` (`matchEventType`) - Obbligatorietà: obbligatoria.
- `team` (`teamMinDto`) - Obbligatorietà: opzionale.
- `lineupPlayer` (`lineupPlayerDto`) - Obbligatorietà: opzionale.
- `lineupPlayer2` (`lineupPlayerDto`) - Obbligatorietà: opzionale.
- `eventImageUrl` (`string`) - Obbligatorietà: obbligatoria. URL icona evento.
- `eventDescription` (`string`) - Obbligatorietà: obbligatoria. Testo telecronaca generato.
- `totalReactions` (`number`) - Obbligatorietà: obbligatoria.
- `totalComments` (`number`) - Obbligatorietà: obbligatoria.
- `reactions` (`Record<string, number>`) - Obbligatorietà: opzionale. Mappa tipo reazione → conteggio.
- `comments` (`matchCommentDto[]`) - Obbligatorietà: opzionale.
- `territory` (`number`) - Obbligatorietà: opzionale.

### Enum usati

- `matchEventType` — usato da `type`

### Consumer FE

- `matchDto.events`, `MatchEventsComponent`, `GMatchEventsComponent`

### API correlate

- Parte di `matchDto` (MTC-02)

### Note di confidenza

Verificato dal codice

---

## matchEventAddDto / matchReactionAddDto / matchCommentAddDto

### Nome nel codice

`matchEventAddDto`, `matchReactionAddDto`, `matchCommentAddDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchEventDto.ts`

### Tipo

DTO request

### Proprietà di matchEventAddDto

- `teamId` (`string`) - Obbligatorietà: opzionale.
- `lineupPlayerId` (`string`) - Obbligatorietà: opzionale.
- `lineupPlayer2Id` (`string`) - Obbligatorietà: opzionale.
- `type` (`matchEventType`) - Obbligatorietà: obbligatoria.
- `subType` (`string`) - Obbligatorietà: opzionale.
- `territory` (`territoryType`) - Obbligatorietà: opzionale.
- `extraParams` (`string`) - Obbligatorietà: opzionale.
- `comment` (`string`) - Obbligatorietà: opzionale.
- `useAiEmoji` (`boolean`) - Obbligatorietà: opzionale.

### Proprietà di matchReactionAddDto

- `eventId` (`string`) - Obbligatorietà: obbligatoria.
- `reactionType` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di matchCommentAddDto

- `eventId` (`string`) - Obbligatorietà: obbligatoria.
- `message` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `MatchService.addEvent`, `MatchService.addReaction`, `MatchService.addComment`
- `MatchEventsComponent`, `MatchPage`

### API correlate

- EVT-03, MTC-05, MTC-06

### Note di confidenza

Verificato dal codice

---

## matchCommentDto

### Nome nel codice

`matchCommentDto`

### Nome wiki suggerito

`matchCommentDto (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchCommentDto.ts`

### Tipo

DTO response

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `user` (`userMinDto`) - Obbligatorietà: obbligatoria. Autore del commento.
- `message` (`string`) - Obbligatorietà: obbligatoria.
- `ts` (`string`) - Obbligatorietà: obbligatoria. Timestamp ISO.

### Consumer FE

- `matchEventDto.comments`, `matchDto.comments`, `MatchEventsComponent`

### Note di confidenza

Verificato dal codice

---

## matchScoreboardDto / matchScoreboardItemDto / matchScoreboardTimelineItemDto

### Nome nel codice

`matchScoreboardDto`, `matchScoreboardItemDto`, `matchScoreboardTimelineItemDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchScoreboardDto.ts`

### Tipo

DTO response

### Sintesi tecnica

Tabellino completo della partita (squadre, giocatori, timeline eventi di punteggio). Usato da `MatchStatsComponent` e da `AiOllamaService` (backend) per generazione blog.

### Proprietà di matchScoreboardDto

- `homeTeam` (`matchScoreboardItemDto`) - Obbligatorietà: opzionale.
- `awayTeam` (`matchScoreboardItemDto`) - Obbligatorietà: opzionale.
- `homeTeamPlayers` (`matchScoreboardItemDto[]`) - Obbligatorietà: opzionale.
- `awayTeamPlayers` (`matchScoreboardItemDto[]`) - Obbligatorietà: opzionale.
- `timeline` (`matchScoreboardTimelineItemDto[]`) - Obbligatorietà: opzionale.

### Proprietà di matchScoreboardItemDto

- `id` (`string`) - Obbligatorietà: opzionale.
- `name` (`string`) - Obbligatorietà: opzionale.
- `score` (`number`) - Obbligatorietà: obbligatoria.
- `tryCount` (`number`) - Obbligatorietà: obbligatoria.
- `conversionCount` (`number`) - Obbligatorietà: obbligatoria.
- `conversionFailedCount` (`number`) - Obbligatorietà: obbligatoria.
- `penaltyCount` (`number`) - Obbligatorietà: obbligatoria.
- `penaltyFailedCount` (`number`) - Obbligatorietà: obbligatoria.
- `yellowCardCount` (`number`) - Obbligatorietà: obbligatoria.
- `redCardCount` (`number`) - Obbligatorietà: obbligatoria.
- `injuryCount` (`number`) - Obbligatorietà: obbligatoria.
- `hasEvent` (`boolean`) - Obbligatorietà: obbligatoria.

### Proprietà di matchScoreboardTimelineItemDto

- `minute` (`number`) - Obbligatorietà: obbligatoria.
- `firstHalf` (`boolean`) - Obbligatorietà: obbligatoria.
- `teamId` (`string`) - Obbligatorietà: opzionale.
- `teamName` (`string`) - Obbligatorietà: opzionale.
- `lineupPlayerId` (`string`) - Obbligatorietà: opzionale.
- `playerName` (`string`) - Obbligatorietà: opzionale.
- `playerNumber` (`string`) - Obbligatorietà: opzionale.
- `eventType` (`string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `matchDto.scoreboard`, `MatchStatsComponent`

### Note di confidenza

Verificato dal codice

---

## matchTeamStatsDto / matchStatsDto / matchStatsUpdateDto

### Nome nel codice

`matchTeamStatsDto`, `matchStatsDto`, `matchStatsUpdateDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchTeamStatsDto.ts`

### Tipo

DTO response / DTO request

### Proprietà di matchTeamStatsDto

- `possession` (`number`) - Obbligatorietà: obbligatoria.
- `penaltyGoals` (`number`) - Obbligatorietà: obbligatoria.
- `conversionGoals` (`number`) - Obbligatorietà: obbligatoria.

### Proprietà di matchStatsDto

- `zone1..zone4` (`number`) - Obbligatorietà: obbligatoria (4 zone campo).

### Proprietà di matchStatsUpdateDto (request)

- `teamId` (`string`) - Obbligatorietà: opzionale.
- `lineupPlayerId` (`string`) - Obbligatorietà: opzionale.
- `status` (`number`) - Obbligatorietà: opzionale.
- `minute` (`number`) - Obbligatorietà: obbligatoria.
- `homePossession` (`number`) - Obbligatorietà: obbligatoria.
- `awayPossession` (`number`) - Obbligatorietà: obbligatoria.
- `zone1..zone4` (`number`) - Obbligatorietà: obbligatoria (4 zone).
- `extraParams` (`string`) - Obbligatorietà: opzionale.
- `comment` (`string`) - Obbligatorietà: opzionale.
- `halfMinutes` (`number`) - Obbligatorietà: opzionale.

### Consumer FE

- `matchDto.homeStats`, `matchDto.awayStats`, `matchDto.stats`
- `MatchService.updateStats` → `MatchPage`

### API correlate

- MTC-04

### Note di confidenza

Verificato dal codice

---

## teamMinDto / teamDto / teamChannelDto / teamPublicDto / teamAddDto / teamUpdateDto / teamLogoDto

### Nome nel codice

Vari (file `teamDto.ts`)

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/teamDto.ts`

### Tipo

DTO response / DTO request

### Proprietà di teamMinDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `channelId` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: obbligatoria.
- `logoUrl` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di teamDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: obbligatoria.
- `logoUrl` (`string`) - Obbligatorietà: obbligatoria.
- `nickname` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di teamChannelDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: obbligatoria.
- `logoUrl` (`string`) - Obbligatorietà: obbligatoria.
- `nickname` (`string`) - Obbligatorietà: obbligatoria.
- `players` (`playerDto[]`) - Obbligatorietà: obbligatoria.
- `matchesPlayed` (`number`) - Obbligatorietà: opzionale.

### Proprietà di teamPublicDto

- `id`, `name`, `logoUrl`, `nickname` (`string`) - Obbligatorietà: obbligatoria.
- `players` (`playerDto[]`) - Obbligatorietà: obbligatoria.
- `channel` (`channelMinDto`) - Obbligatorietà: obbligatoria.

### Proprietà di teamAddDto / teamUpdateDto

- `name` (`string`) - Obbligatorietà: obbligatoria.
- `nickname` (`string | null`) - Obbligatorietà: opzionale.
- `logoUrl` (`string`) - Obbligatorietà: opzionale.

### Proprietà di teamLogoDto

- `Record<string, string>` — mappa logo disponibili

### Consumer FE

- `matchDto.homeTeam/awayTeam`, `ChannelTeamsComponent`, `GTeamPage`
- `TeamService`, `TeamEditComponent`

### API correlate

- TMS-01..06

### Note di confidenza

Verificato dal codice

---

## playerDto / playerSaveDto

### Nome nel codice

`playerDto`, `playerSaveDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/playerDto.ts`

### Tipo

DTO response / DTO request

### Proprietà di playerDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `name` (`string`) - Obbligatorietà: opzionale.
- `nickname` (`string`) - Obbligatorietà: opzionale.
- `avatarUrl` (`string`) - Obbligatorietà: opzionale.
- `defaultNumber` (`number | null`) - Obbligatorietà: opzionale.

### Proprietà di playerSaveDto

- `name` (`string | null`) - Obbligatorietà: opzionale.
- `nickname` (`string | null`) - Obbligatorietà: opzionale.
- `avatarUrl` (`string | null`) - Obbligatorietà: opzionale.
- `defaultNumber` (`number | null`) - Obbligatorietà: opzionale.

### Consumer FE

- `lineupPlayerDto.player`, `teamChannelDto.players`, `teamPublicDto.players`
- `MatchLineupComponent`, `GTeamPage`, `PlayerEditComponent`
- `PlayerService`, `MatchPage`

### API correlate

- PYR-01, PYR-03, PYR-04, PYR-05

### Note di confidenza

Verificato dal codice

---

## lineupPlayerDto / lineupPlayerEditDto / lineupPlayerAddDto / lineupPlayerEventDto

### Nome nel codice

`lineupPlayerDto`, `lineupPlayerEditDto`, `lineupPlayerAddDto`, `lineupPlayerEventDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/lineupPlayerDto.ts`

### Tipo

DTO response / DTO request

### Proprietà di lineupPlayerDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `player` (`playerDto`) - Obbligatorietà: obbligatoria.
- `number` (`number`) - Obbligatorietà: obbligatoria. Numero di ruolo (1–23).
- `rate` (`number`) - Obbligatorietà: obbligatoria. Voto medio ricevuto.
- `events` (`lineupPlayerEventDto[]`) - Obbligatorietà: obbligatoria. Icone eventi.

### Proprietà di lineupPlayerEventDto

- `eventImageUrl` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di lineupPlayerEditDto

- `playerId` (`string`) - Obbligatorietà: opzionale.

### Proprietà di lineupPlayerAddDto

- `name` (`string`) - Obbligatorietà: opzionale.
- `nickname` (`string`) - Obbligatorietà: opzionale.
- `defaultNumber` (`number`) - Obbligatorietà: opzionale.

### Consumer FE

- `matchDto.homeTeamLineup/awayTeamLineup`, `matchEventDto.lineupPlayer`
- `MatchLineupComponent`, `LineupService`

### API correlate

- LNP-04, LNP-05, LNP-06

### Note di confidenza

Verificato dal codice

---

## blogDto / matchBlogDto / teamBlogDto

### Nome nel codice

`blogDto`, `matchBlogDto`, `teamBlogDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/blogDto.ts`

### Tipo

DTO response

### Proprietà di blogDto

- `title` (`string`) - Obbligatorietà: obbligatoria.
- `body` (`string`) - Obbligatorietà: obbligatoria.
- `match` (`matchBlogDto`) - Obbligatorietà: obbligatoria. (Non sempre valorizzato: nel `getMatchBlog` il campo `match` è `null`)

### Proprietà di matchBlogDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `date` (`string`) - Obbligatorietà: obbligatoria.
- `homeTeam` (`teamBlogDto`) - Obbligatorietà: obbligatoria.
- `awayTeam` (`teamBlogDto`) - Obbligatorietà: obbligatoria.

### Proprietà di teamBlogDto

- `name` (`string`) - Obbligatorietà: obbligatoria.
- `logoUrl` (`string`) - Obbligatorietà: obbligatoria.
- `score` (`number`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `GMatchPage`, `MatchPage`, `MatchEventsComponent`
- `BlogService.getBlogs`, `BlogService.getMatchBlog`

### API correlate

- BLOG-01, BLOG-02

### Note di confidenza

Verificato dal codice

---

## statsDto / statsEventTypeDto

### Nome nel codice

`statsDto`, `statsEventTypeDto`

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/statsDto.ts`

### Tipo

DTO response

### Proprietà di statsDto

- `users` (`number`) - Obbligatorietà: obbligatoria.
- `channels` (`number`) - Obbligatorietà: obbligatoria.
- `matches` (`number`) - Obbligatorietà: obbligatoria.
- `teams` (`number`) - Obbligatorietà: obbligatoria.
- `players` (`number`) - Obbligatorietà: obbligatoria.
- `events` (`number`) - Obbligatorietà: obbligatoria.
- `eventsByType` (`statsEventTypeDto[]`) - Obbligatorietà: obbligatoria.
- `emojis` (`number`) - Obbligatorietà: obbligatoria.
- `comments` (`number`) - Obbligatorietà: obbligatoria.

### Proprietà di statsEventTypeDto

- `type` (`string`) - Obbligatorietà: obbligatoria.
- `value` (`number`) - Obbligatorietà: obbligatoria.
- `count` (`number`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `GStatsPage`, `StatsService.getStats`

### API correlate

- `StatsV1Controller.GetStats` — STS-01

### Note di confidenza

Verificato dal codice

---

## messageDraftDto / messageDraftItemDto / messageDraftItemEditDto / messageStatisticsDto

### Nome nel codice

Vari (file `adminDto.ts`)

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/adminDto.ts`

### Tipo

DTO response / DTO request

### Sintesi tecnica

DTO per gli endpoint admin (messaggi di sistema in draft). Consumer FE non deducibile dai RAW analizzati — probabilmente usato da un pannello admin non incluso nelle route principali.

### Proprietà di messageDraftDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `code` (`string`) - Obbligatorietà: obbligatoria.
- `groupId` (`string`) - Obbligatorietà: obbligatoria.
- `language` (`string`) - Obbligatorietà: obbligatoria.
- `it`, `en`, `ja`, `fr`, `es` (`messageDraftItemDto`) - Obbligatorietà: obbligatoria. Un campo per lingua.

### Proprietà di messageDraftItemDto

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `message` (`string`) - Obbligatorietà: obbligatoria.
- `verifyValue` (`number`) - Obbligatorietà: obbligatoria.
- `verifyLanguageValue` (`number`) - Obbligatorietà: obbligatoria.
- `isVerified` (`boolean`) - Obbligatorietà: obbligatoria.
- `isDeleted` (`boolean`) - Obbligatorietà: obbligatoria.
- `flagPlayer` (`boolean`) - Obbligatorietà: obbligatoria.

### Proprietà di messageDraftItemEditDto

- `message` (`string`) - Obbligatorietà: obbligatoria.

### Proprietà di messageStatisticsDto

- `id` (`number`) - Obbligatorietà: obbligatoria.
- `code` (`string`) - Obbligatorietà: obbligatoria.
- `count` (`number`) - Obbligatorietà: obbligatoria.
- `sysMessageCount` (`number`) - Obbligatorietà: obbligatoria.
- `average` (`number`) - Obbligatorietà: obbligatoria.

### Consumer FE

Non deducibile (nessun servizio FE che chiami gli endpoint admin ADM-* è stato trovato nel codebase analizzato)

### API correlate

- `AdminV1Controller` — ADM-01..05

### Note di confidenza

Parzialmente dedotto (consumer non verificato)

---

## Modelli frontend (view model e tipi UI)

---

## EntityTab

### Nome nel codice

`EntityTab`

### Nome wiki suggerito

`EntityTab (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/common/entity-tabs/entity-tabs.component.ts`

### Tipo

Interface — view model componente

### Sintesi tecnica

Definizione di una singola tab nella barra di navigazione tabbed.

### Proprietà

- `id` (`string`) - Obbligatorietà: obbligatoria.
- `label` (`string`) - Obbligatorietà: obbligatoria. Chiave i18n o label diretta.
- `icon` (`string`) - Obbligatorietà: obbligatoria. Nome icona.

### Consumer FE

- `EntityTabsComponent`
- `GChannelPage`, `GMatchPage`, `GTeamPage`, `ChannelPage`, `MatchPage`

### Note di confidenza

Verificato dal codice

---

## StatsGridItem

### Nome nel codice

`StatsGridItem`

### Nome wiki suggerito

`StatsGridItem (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/common/entity-stats-grid/entity-stats-grid.component.ts`

### Tipo

Interface — view model componente

### Proprietà

- `icon` (`string`) - Obbligatorietà: obbligatoria.
- `label` (`string`) - Obbligatorietà: obbligatoria.
- `value` (`number | string`) - Obbligatorietà: obbligatoria.

### Consumer FE

- `EntityStatsGridComponent`
- `GChannelPage`, `GTeamPage`, `ProfilePage`, `HomeUserComponent`

### Note di confidenza

Verificato dal codice

---

## ButtonVariant / ButtonColor

### Nome nel codice

`ButtonVariant`, `ButtonColor`

### Nome wiki suggerito

`ButtonVariant (web)`, `ButtonColor (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/common/entity-button/entity-button.component.ts`

### Tipo

Type alias — UI

### Valori di ButtonVariant

- `'pill'` — bottone arrotondato (CTA inline/footer)
- `'panel'` — bottone rettangolare full-width con icona sinistra
- `'close'` — bottone X per chiusura modal/drawer

### Valori di ButtonColor

- `'primary'` — fill colorato
- `'secondary'` — sfondo bianco + bordo
- `'ghost'` — trasparente + bordo sottile

### Consumer FE

- `EntityButtonComponent` — input `variant`, `color`

### Note di confidenza

Verificato dal codice

---

## SkeletonLayout

### Nome nel codice

`SkeletonLayout`

### Nome wiki suggerito

`SkeletonLayout (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/common/entity-skeleton/entity-skeleton.component.ts`

### Tipo

Type alias — UI

### Valori

- `'header'` — media + testo + azioni
- `'card'` — cover + corpo testo
- `'list'` — righe compatte
- `'text'` — solo testo

### Consumer FE

- `EntitySkeletonComponent` — input `layout`

### Note di confidenza

Verificato dal codice

---

## Toast

### Nome nel codice

`Toast`

### Nome wiki suggerito

`Toast (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/services/toast.service.ts`

### Tipo

Interface — view model servizio

### Proprietà

- `id` (`number`) - Obbligatorietà: obbligatoria. ID univoco auto-incrementale.
- `type` (`ToastType`) - Obbligatorietà: obbligatoria.
- `text` (`string`) - Obbligatorietà: obbligatoria.

### Enum usati

- `ToastType` — usato da `type`

### Consumer FE

- `ToastService`, `EntityToastComponent`

### Note di confidenza

Verificato dal codice

---

## PlayerSaveData

### Nome nel codice

`PlayerSaveData`

### Nome wiki suggerito

`PlayerSaveData (web)`

### File sorgente

- `src/RugbyRadioWeb/src/app/common/player-edit/player-edit.component.ts`

### Tipo

Interface — view model componente (output evento)

### Proprietà

- `name` (`string | null`) - Obbligatorietà: opzionale.
- `nickname` (`string | null`) - Obbligatorietà: opzionale.
- `avatarUrl` (`string | null`) - Obbligatorietà: opzionale.
- `defaultNumber` (`number | null`) - Obbligatorietà: opzionale.

### Consumer FE

- `PlayerEditComponent` — output evento `savePlayer`

### Note di confidenza

Verificato dal codice

---

## Enum frontend

---

## matchStatus

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchDto.ts`

### Valori

- `scheduled = '10'` — Partita programmata
- `inProgress = '20'` — Partita in corso
- `halftime = '25'` — Intervallo
- `fullTime = '30'` — Partita terminata

### Usato da

- `matchDto.status.id`, `matchMinDto.status.id`
- `MatchCardComponent.showLiveButton()`
- `MatchCardSmallComponent.isScheduled`
- `GMatchPage`, `MatchPage` — condizioni stato UI

---

## matchEventType

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchEventDto.ts`

### Valori (principali)

- `kickoff = 100`, `kickoffSecondHalf = 110`
- `fulltime = 200`
- `try = 300`, `conversion = 400`, `conversionFailed = 500`
- `penalty = 600`, `penaltyFailed = 700`
- `tryPenalty = 800`
- `signal = 900`
- `scrum = 1000`, `touche = 1100`, `injury = 1200`, `kick = 1300`
- `yellowCard = 1400`, `redCard = 1500`
- `halftime = 1600`, `war = 1700`, `forwardPass = 1800`, `tenMt = 1900`
- `toucheFoul = 2000`, `offside = 2100`, `substitution = 2200`
- `headset = 2300`, `errataCorrige = 2400`, `tackle = 2500`, `heldUp = 2600`

### Usato da

- `matchEventDto.type`
- `matchEventAddDto.type`
- `MatchEventsComponent` — gestione input eventi

---

## territoryType

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchEventDto.ts`

### Valori

- `zone1 = 10`
- `zone2 = 20`
- `zone3 = 30`
- `zone4 = 40`

### Usato da

- `matchEventAddDto.territory`
- `MatchEventsComponent` — selezione zona campo

---

## weatherType

### File sorgente

- `src/RugbyRadioWeb/src/app/dto/matchEventDto.ts`

### Valori

- `sunny = 101`
- `partlyCloudy = 102`
- `cloudly = 103`
- `rain = 104`
- `snow = 105`
- `fog = 106`

### Usato da

- Sottotipo degli eventi meteo in `matchEventAddDto.type`

---

## ToastType

### File sorgente

- `src/RugbyRadioWeb/src/app/services/toast.service.ts`

### Valori

- `'success'`
- `'info'`
- `'warning'`
- `'error'`

### Usato da

- `Toast.type`, `ToastService.show(type, text)`, `EntityToastComponent`

---

## Note finali

- Limiti dell'analisi: i consumer dei `adminDto.ts` non sono stati verificati (nessun servizio FE trovato che chiami gli endpoint ADM-*); alcuni tipi view model definiti inline in componenti non letti nel dettaglio (es. `commentatorDto`, `languageTab` in `MatchCommentatorComponent`, `HomeListConfig` in `home-list.config.ts`) non sono stati inclusi.
- Elementi non deducibili: consumer FE di `messageDraftDto` (endpoint admin non trovati in alcun servizio FE); eventuali type alias o interfacce definite inline nei componenti `user/match/*` e `user/channel/*` non analizzati.
- Possibili approfondimenti: lettura di `home-list.config.ts`; analisi dei componenti `user/match/*` per tipi aggiuntivi; verifica utilizzo di `adminDto.ts` nel codebase.
- Wiki non modificata: confermato.
